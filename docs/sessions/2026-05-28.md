date: 2026-05-28
commits: 2
areas: [assets, monorepo, web, product]

decisions:

- @dayopt/assets パッケージを新設し、Symbol（SVG コンポーネント）と Wordmark（brand name テキスト）を集約。@dayopt/ui の Logo から参照する構造へ変更
- Symbol / Wordmark のレンダリング用コンポーネントと合わせて SYMBOL_PATH_D / SYMBOL_VIEW_BOX / BRAND_WORDMARK を定数 export する設計を採用。next/og の制約下でも同じアセットを参照できるようにした

learned:

- 'next/og の ImageResponse は通常の React component + Tailwind クラスを解釈できない。SVG path 定数（SYMBOL_PATH_D）を @dayopt/assets から export しておくことで OG image からも同じアセットを利用できる'

files_of_note:

- packages/assets/src/symbol.tsx # 新規: Symbol SVG コンポーネント + SYMBOL_PATH_D / SYMBOL_VIEW_BOX 定数
- packages/assets/src/wordmark.tsx # 新規: Wordmark テキストコンポーネント + BRAND_WORDMARK 定数
- packages/ui/src/logo.tsx # @dayopt/assets の Symbol / Wordmark を参照する構造に変更
- apps/product/src/app/opengraph-image.tsx # OG logo を文字 "D" → SYMBOL_PATH_D 直参照 SVG に変更

next:

- [ ] @dayopt/ui の product 実利用を他コンポーネントにも展開（Button / Badge 以外の shadcn primitives）
- [ ] packages/config の残り hardcode 置換
- [ ] Storybook に @dayopt/assets の Symbol / Wordmark story を追加
