date: 2026-05-21
commits: 13
areas: [packages, chronotype, calendar, tags, review, entry, storybook, quality]

decisions:

- shared UI primitives package を新設 (#fdeed7)。`packages/ui` として workspace に追加
- design token package を新設 (#1192)。`packages/design-tokens` として workspace に追加し、token は package から import する方針
- chronotype state を `features/chronotype/stores` へ切り出し (#1190)。global lib/stores から feature ownership へ移動
- calendar の view range / precision policy を `features/calendar/domain` へ切り出し (#1186)
- calendar interaction の純粋部分を `features/calendar/domain/interaction` へ抽出 (#1187)
- review の variance 計算を `features/review/domain` へ切り出し (#1191)
- tags の純粋ルールを `features/tags/domain` へ集約 (#1193)
- entry の時間更新ルールを `features/entry/domain` へ寄せる (#1185)
- `useCalendarFilterStore` を `features/calendar/stores` へ移動 (#1195)
- chronotype の `onChronotypeChange` callback 注入パターンを廃止、直接 store を呼ぶ (#1196)
- quality reports 生成を削除 (#1188)。dead pipeline の cleanup

breaking:

- chronotype の callback 注入 API を廃止。store を直接呼び出す形に統一
- quality reports 生成パイプラインを廃止

files_of_note:

- packages/ui/ # 新規 shared UI primitives
- packages/design-tokens/ # 新規 design token package
- apps/product/src/features/chronotype/stores/ # global store からの移動先
- apps/product/src/features/calendar/domain/ # view range / interaction の domain 層
- apps/product/src/features/review/domain/ # variance 計算
- apps/product/src/features/tags/domain/ # 純粋ルール
- apps/storybook/docs/ # packages 設計方針ドキュメントを集約

next:

- [ ] chronotype hook 化（NowBadge / Calendar で profile 計算が残っている）
- [ ] tag-service.ts の merge / ungroupTags から pure logic 切り出し
- [ ] entry statistics.ts の domain 切り出し（次の大きな塊）
