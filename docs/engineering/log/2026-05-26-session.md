date: 2026-05-26
commits: 15
areas: [packages, billing, database, entry, tags, auth, types, deps, monorepo]

decisions:

- billing model package を新設 (#1221)。`@dayopt/billing` として workspace に追加
- Supabase database package を新設 (#1219)。`@dayopt/database` として workspace に追加し、Supabase 関連型と client を集約
- entry statistics.ts から pure aggregation を `features/entry/domain` へ切り出し (#1223)
- statistics.ts から monthly-trend aggregation を domain へ切り出し (#1224)
- statistics.ts から estimation-accuracy transformer を domain へ切り出し (#1225)
- statistics.ts から getTagStats aggregation を domain へ切り出し (#1226)
- `getStatsOverview` の RPC response unpacking を server helper へ切り出し (#1227)
- `getTimeByTag` transformer を server helper へ切り出し (#1229)
- tag-service.ts の merge / ungroupTags から pure logic を切り出し (#1222)
- product の auth boundaries を整理 (#1230)。protectedProcedure / publicProcedure の境界を明示
- shared package integration の audit を実施 (#1228)。各 package の dependency を見直し
- `lib/types/settings.ts` の残存理由を inline docs で明記 (#1220)

breaking:

- billing 関連型は `@dayopt/billing` 経由で import するよう変更
- Supabase client / 型は `@dayopt/database` 経由で import するよう変更

learned:

- statistics.ts のような god module は「pure aggregation → transformer → RPC unpacking → server helper」の順で外側から剥がすと安全。1 PR 1 関数の粒度で進めると review もしやすく、回帰リスクが分散する

files_of_note:

- packages/billing/ # 新規 billing model package
- packages/database/ # 新規 Supabase database package
- apps/product/src/features/entry/domain/ # statistics.ts から切り出した pure 部分の置き場
- apps/product/src/features/entry/server/ # server helper の置き場
- apps/product/src/features/tags/domain/ # tag-service.ts から切り出した pure 部分

next:

- [ ] entry statistics.ts の残り inline RPC unpacking を完全クリーンアップ
- [ ] monorepo package foundation の readiness 仕上げ（次の package 追加に向けた整地）
- [ ] auth boundaries 整理の他 router への展開
