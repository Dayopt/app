'use client';

/**
 * AI モード Sidebar 中身 (Step C-2 時点では stub)
 *
 * Step C-3 で phase-2-c-detail.md §5.2 の 3 ブロック構成
 * (タイトル / 空 Conversations list / Soon セクション) に充実させる。
 */
export function AiSidebar() {
  return <div className="text-muted-foreground px-4 py-4 text-sm">AI mode placeholder</div>;
}
