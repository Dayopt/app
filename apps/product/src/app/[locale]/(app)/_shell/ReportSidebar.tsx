/**
 * Report タブの Sidebar 本体（Composition Layer、スクロール領域）
 *
 * v1 は placeholder（Step 4 で本体を実装する）。MiniCalendar は Sidebar の
 * pinned 領域（プロフィール直上）へ移動した（#2217）。セグメント一覧は
 * Step 5（セグメント配線）で追加する
 * （docs/projects/workspace-shell-restructure/overview.md §5-5）。
 */
export function ReportSidebar() {
  return null;
}
