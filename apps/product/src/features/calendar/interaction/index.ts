/**
 * Interaction Module — 統合インタラクションシステム（実利用 API のみ）
 */

// Types
export type { InteractionState } from '../domain/interaction/types';

// React hook
export { useInteraction } from './useInteraction';
