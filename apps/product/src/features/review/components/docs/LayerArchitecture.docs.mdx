import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Product/Features/Review/Guide/Layer Architecture" />

# Layer Architecture

Review panel の技術的な分層設計。Reflection と Diff を同じ Review 所有の panel segment として扱う。

---

## 2 segment model

```
Reflection  週次 Review。getStatsPageData / getTimePL から予実とタグ差分を表示
Diff        日次差分。Calendar entries から予定外・未実施・移動・長さ変更を表示
```

### Data vs Panel Segment

data source と panel segment は**直交する**概念。

|                       | Reflection                       | Diff                      |
| --------------------- | -------------------------------- | ------------------------- |
| **RPC**               | `getStatsPageData` / `getTimePL` | なし                      |
| **Client derivation** | Time P/L derivers                | `computeCalendarDayDiffs` |
| **UI**                | `CalendarReviewPanel`            | `CalendarDayDiffRail`     |

runtime では Reflection と Diff がまだ別 `panel` state だが、Storybook 上は Review が両 segment を所有する。

---

## Reflection

週次 Review の compact panel。LLM は使わず、既存 RPC と純粋 deriver で表示する。

- **KPI**: 記録時間、計画達成率、予定、差分
- **Time P/L**: `deriveStatement` / `deriveAccuracy` / `deriveBarComparison`
- **計算場所**: `features/entry` の summary RPC + `features/review/domain`
- **AI 依存**: なし
- **対象**: 全ユーザー（Free 含む）

詳細な catalog は現行 Storybook taxonomy では個別 story に集約する。

---

## Diff

日次の予定と実績の差分を Calendar entries から導出する。

- **Summary**: planned / actual / diff / unplanned / missed
- **List**: unplanned / missed / shifted / resized
- **計算場所**: `features/calendar/lib/day-diff.ts`
- **AI 依存**: なし
- **対象**: day view

詳細な catalog は現行 Storybook taxonomy では個別 story に集約する。

---

## Not In V1

- Analytics panel は v1 の主要構成に含めない。
- LLM insight は扱わない。
- `CalendarDayDiffRail` の物理移動は、Calendar 内部型への依存を外す adapter 抽出後に扱う。

---

## Feature Boundary

```
entry feature     → DB 集約（RPC 呼び出し）を所有
review feature    → 表示ロジック・UIを所有
settings feature  → AI 設定（communication style, values）を所有
supabase/functions/ → Edge Function（プラットフォーム基盤）
```

Feature 間の直接 import は公開 API 経由に限定する。`CalendarDayDiffRail` は現時点では Calendar 内部に残し、Storybook title だけ Review / Diff として扱う。

---

**最終更新**: 2026-03-16
