date: 2026-05-12
commits: 1
areas:

- review/tags (ReviewTagList / ReviewTagActionsMenu / ReviewTagChipRow 新設)
- db/functions (get_time_pl_data を entries.tag_id 参照に再作成)
- architecture/docs (ServiceAudit / ServiceContracts / api-shape.md 追加)

decisions:

- get_time_pl_data を entries.tag_id 参照で再作成。20260415 で entry_tags を削除したが 20260416 の migration がその関数を古い entry_tags join で recreate していた。正しい参照先に差し替える migration（20260512）を追加
- ReviewTagList（サイドバー）・ReviewTagActionsMenu・ReviewTagChipRow（モバイル）を専用コンポーネントとして新設。ReviewView の肥大化を解消し、タグ操作 UI を独立させた

learned:

- 後続 migration で `CREATE OR REPLACE FUNCTION` すると、先行 migration が行った schema 変更（entry_tags 削除など）を考慮しない古い実装で関数を上書きしてしまう。migration は「その時点のスキーマ状態」に対して正しい SQL を書く必要がある

files_of_note:

- supabase/migrations/20260512000000_fix_time_pl_inline_tag_id.sql # get_time_pl_data の entry_tags → entries.tag_id 修正
- src/features/review/components/sidebar/ReviewTagList.tsx # 新規 765 lines — サイドバータグ操作 UI
- .storybook/docs/dev/architecture/ServiceAudit.mdx # Service 層の現状の歪みマップ
- .storybook/docs/dev/architecture/ServiceContracts.mdx # target service 契約定義

next:

- [ ] Phase 1.5: issueTokenPair の 2 insert を Postgres RPC で atomic 化
- [ ] Phase 1.5: /oauth/authorize の error redirect 実装
- [ ] Phase 1.5: OAuth token revoke UI
- [ ] /review Storybook story 整備
