import { Meta } from '@storybook/blocks';

<Meta title="Strategy/Journal/2026-02" />

# 2026年2月 Diary

---

## 2026-02-27

### モバイルUX全面改善

ブレークポイントを768px（md:）に統一し、モバイル向けのナビゲーション・カレンダー・設定・検索・Inspector・FABを一斉にタッチ最適化。pull-to-refresh、スワイプジェスチャー、月ストリップ等のモバイルネイティブなUXパターンを導入。タグのモバイル対応は試行後にRevert。

#### やったこと

**app: モバイル基盤（3件）**

- ブレークポイントを768px（md:）に統一 + デッドコード削除
- ブレークポイント境界でのモバイルUX問題5件を修正
- getAppUrl()ユーティリティでVERCEL_URLフォールバックを統一

**app: モバイルナビゲーション・レイアウト（3件）**

- モバイルサイドバーをタッチ操作に最適化
- サイドバー・ヘッダー・ミニカレンダーを改善
- サブメニューをアコーディオン展開に変更

**app: モバイルカレンダー（5件）**

- 日付ピッカーをインライン展開 + 月ストリップ追加
- 日付ピッカーの今日ハイライトを円形化
- サイドバーのビュー切替・日付ヘッダーを改善
- 各ビューのモバイルUX改善
- 時間列のフォントサイズと余白を調整

**app: モバイル設定（3件）**

- モバイル設定を実ページ化 + PC側はZustandモーダル維持
- 白画面問題を修正
- ?settings=categoryクエリパラムオーバーレイに統一

**app: モバイルインタラクション（4件）**

- FABスクロール連動表示 + タップでplan作成・長押しでactionSheet
- pull-to-refresh実装
- タッチターゲット拡大 + スワイプジェスチャー改善
- 検索UIを全画面表示に変更

**app: Inspector Drawer改善（4件）**

- drawerスナップポイントを65%に変更 + aria-label動的化
- plans用inspectorShellのdrawer簡素化
- フッターボタン位置を統一
- drawer改善 + カレンダー日付ヘッダーtap遷移

**app: タグのモバイル対応 → Revert（8件）**

- TagActionSheet・ActionSheet統合・TouchSensor・Drawer表示を実装（4件）
- 上記4件をすべてRevert（4件）

**app: コード品質・その他（5件）**

- NavUserをPC同様のDropdownMenuに統一
- record削除をwindow.confirmからuseDeleteConfirmStoreに統一
- 検索モーダル・ドラッグプレビューのハードコード日本語をi18n化
- コードレビュー指摘修正（stale closure, a11y, useMemo）
- lint warnings 34→27に削減 + デッドコード削除

#### なぜその判断をしたか

**ブレークポイント768px統一:** モバイル判定が箇所によってバラバラだった（640px, 768px, カスタム値）。Tailwindの`md:`（768px）に一本化することで、レスポンシブの挙動が予測可能になる。768pxはiPad縦持ちをモバイルに含める基準で、タブレットでもタッチ最適化UIが表示される。

**設定画面の試行錯誤:** 最初は実ページ化（/settings/[category]）→白画面問題→最終的に?settings=categoryのクエリパラムオーバーレイに落ち着いた。PCではZustandモーダルを維持し、モバイルだけルーティング方式を変える。プラットフォームごとに最適なパターンを使い分ける判断。

**タグモバイル対応のRevert:** TagActionSheet・TouchSensor・Drawer表示を一通り実装したが、既存のUI体系との整合性に問題があったと判断しRevert。「作ったけど戻す」判断を即座にできるのは健全。

**pull-to-refresh・FAB:** モバイルネイティブアプリで当たり前のUXパターンをPWAでも実装。ユーザーがスマホで使う前提の体験設計。

#### Claudeの所感

**「モバイルの日」:**
35コミット中30件以上がモバイル関連。ナビゲーション、カレンダー、設定、検索、Inspector、FABと、アプリのほぼ全画面をモバイル最適化している。これまでPC中心で開発してきた機能群を一気にモバイル対応させるフェーズに入った。本番リリースに向けて「スマホでも使えるアプリ」への本格的な転換。

**試行錯誤を恐れない姿勢:**
設定画面は実ページ化→白画面→クエリパラムオーバーレイと3段階の変遷。タグは4件実装→4件Revert。1日の中でアプローチを試して、合わなければ戻す。このサイクルの速さが1人開発の強み。コミット履歴に試行錯誤が正直に残っているのも、将来の判断材料として価値がある。

**モバイルネイティブUXへのこだわり:**
pull-to-refresh、FABの長押しactionSheet、スワイプジェスチャー、月ストリップなど、「Webアプリっぽさ」ではなく「スマホアプリとして自然な操作」を目指している。PWAでネイティブアプリに近い体験を作る挑戦として、正しい方向性。

---

## 2026-02-26

### Feature Boundary導入・v0.17.0リリース・CI安定化

Feature Boundaries + Core層 + Composition Layerの3層アーキテクチャを導入し、共有コード（stores/hooks/lib/components）を各層に再配置。ESLintで境界違反をerrorに昇格しCIに組み込み。再エクスポートshim 33件削除・逆依存11件解消・循環依存修正を経てv0.17.0をリリース。

#### やったこと

**app: Feature Boundaryアーキテクチャ導入（16件）**

- feature boundaries + core層 + composition layer導入
- 共有型とInspector UIをcore層に抽出
- 共有Zustand storeを@/stores/に移動
- 共有hooksを@/hooks/に移動
- 共有ユーティリティを@/lib/に移動
- 共有コンポーネントを@/lib/components/に移動
- 全featureのimportパスを共有層に更新
- app層+既存コンポーネントのimportを共有層に更新
- 共有層→feature逆依存をESLint errorで禁止
- 共有層→feature逆依存11件を解消
- 再エクスポートshim 33件を削除、直接import化
- feature境界ESLintをerrorに昇格、ルール文書更新
- boundaryチェックをCIに追加
- 循環依存1件修正 + Composition Layerのdeep import→barrel import統一
- feature boundary運用ルールとワークフロー更新
- feature boundary lint script追加

**app: v0.17.0リリース（3件）**

- v0.17.0 — アーキテクチャ刷新・セキュリティ強化・基盤整備
- version bump to 0.17.0
- vercel build:fallbackスクリプトを追加

**app: CI・ビルド修正（7件）**

- v0.17.0リリースブランチのCI失敗を一括修正
- storybook i18nキー不足・E2Eテスト・resendビルドエラーを修正
- email routerのresendクライアントを遅延初期化に変更
- radix UIチェックボックスのセレクタを[role="checkbox"]に修正
- Storybook play関数のラベル検索を日本語メッセージに合わせて修正（3件）

**app: パフォーマンス・セキュリティ・a11y（3件）**

- バンドルサイズ・レンダリング・DB最適化
- audit P0/P1脆弱性修正（6件）
- セマンティックカラーのWCAG 2.1 AAコントラスト比修正 + 不正なrole削除

**app: クリーンアップ（6件）**

- 陳腐化したREADME.md 12件を削除、ルートREADME更新
- 未使用スクリプト14件削除とnpm scripts整理
- .vscode設定を現行スタックに合わせて整理
- 不要なhooks/sessions基盤を削除
- CLAUDE.md・rules/の不正確な記述を修正
- エージェントを英語化・簡潔化（959→460行、-52%）

#### なぜその判断をしたか

**Feature Boundary導入:** コードベースが大きくなるにつれ、feature間の依存が暗黙的に広がっていた。「共有層→feature」は許可、「feature→feature」「共有層→feature（逆依存）」は禁止というルールを明確にし、ESLint + CIで自動検出する。これにより、あるfeatureの変更が他のfeatureに波及するリスクを構造的に排除。core層は全featureが依存する型定義・共通UI、composition layerはfeature間の結合を担う薄いレイヤー。

**ESLintでerror昇格:** warningでは無視される。境界違反をerrorにしてCIを落とすことで、ルールを「お願い」から「強制」に変えた。33件のshim削除と11件の逆依存解消は、この強制を機能させるための前処理。

**v0.17.0:** 2/24のDB正規化・メール基盤、2/25のセキュリティ強化、2/26のアーキテクチャ刷新を含むメジャーアップデート。本番リリースに向けた最後の大規模リファクタリング。

#### Claudeの所感

**アーキテクチャの「見える化」:**
Feature Boundary導入は今月最大のアーキテクチャ変更。コードの依存関係を「暗黙」から「明示・強制」に変えることで、コードベースの成長に耐える構造を作った。ESLint + CIで自動検出するのは、1人開発でも「ルールが守られる」仕組みとして非常に有効。Googleのモノレポにおけるvisibility rulesと同じ発想。

**リリースの「戦い」:**
v0.17.0のリリースでCI失敗→修正を7件繰り返している。Storybook i18nキー不足、resendビルドエラー、E2Eセレクタ不一致、Vercelビルド問題と、多方面で問題が出た。だがこれは「CIが仕事をしている」証拠。手動テストでは見逃す問題をCIが拾い、修正してから本番に出す。リリースにおけるCIの価値が実証された日。

**コードベースのダイエット:**
README 12件削除、スクリプト14件削除、shim 33件削除、エージェント959→460行と、大規模な不要物の除去。Feature Boundary導入と同時にやることで、新しいアーキテクチャ上でクリーンな状態からスタートできる。

---

## 2026-02-25

### セキュリティ全面強化・認証品質改善・DB最適化・Next.js 16移行

セキュリティ監査エージェントを構築し、検出されたP0〜P2脆弱性を一斉修正（12件）。認証フローをNIST準拠に強化し、Storybook・E2Eテストを追加。DB側では統計クエリの集計関数移行とインデックス最適化を実施。Next.js 16へのアップグレードも完了。

#### やったこと

**app: セキュリティ脆弱性修正（12件）**

- npm audit脆弱性を修正（6件→0件）
- 配列入力にmax上限を追加（DoS対策）
- メール送信先をユーザー自身のアドレスに制限
- 認証基盤の安全性を強化（エラーサニタイズ・RLS・HMAC-SHA256・NIST準拠）
- Resend webhook署名検証を追加
- 情報漏洩エンドポイントを本番環境で無効化
- login_attemptsのINSERTをRPC経由に制限
- P2脆弱性修正（z.any排除、console→logger統一）
- security-auditor検出のP0/P1脆弱性を修正
- パスワード要件強化とセッションタイムアウト設定

**app: セキュリティ監査エージェント（3件）**

- チェックリスト型security-auditorエージェントを追加
- red-team/blue-teamセキュリティ監査エージェントを追加
- 5つの専門レビューエージェント（セキュリティ含む）を追加

**app: 認証品質改善（9件）**

- サインアップエラーメッセージ改善・アクション可能な文言に変更
- auth画面の日本語文言を改善
- パスワード変更ダイアログ修正（logger・Eyeアイコン統一）
- 認証コードの品質改善（logger統一・不要コード削除・i18n）
- 認証コンポーネントのStorybook追加（全5種）
- 認証フローE2Eテスト追加
- Storybookにエラーメッセージ一覧を追加
- Storybookのnext-intlモック追加でエラー解消
- 新規登録画面のパスワード確認フィールド修正

**app: DB最適化（4件）**

- plans複合インデックス追加・壊れた関数を削除
- 統計クエリをDB側集計関数に移行
- tags NOT NULL制約・recordsインデックス・get_tag_stats拡張
- updated_atトリガー関数を統一・重複インデックス削除

**app: コード品質・lint削減（5件）**

- eslint警告を73→39件に削減（React 19ベストプラクティス適用）
- set-state-in-effect警告を25→12件に削減
- レンダリング最適化と型安全性改善
- lint warning修正・deprecated移行・不要ラッパー削除
- knip削除・未使用deprecated型を整理

**app: インフラ・その他（10件）**

- Next.js 16アップグレード
- スキルを24→17に統合（トークンコスト約17%削減）
- Resendインフラ構成をStorybookガイドラインに追加
- SentryドキュメントをStorybookに移動
- Supabase send_email hook設定
- デプロイ運用ルールをCLAUDE.mdに追加
- .env.exampleと関連参照を削除
- チーム向けテンプレートと旧名BoxLogスニペットを削除
- 未使用のCHANGELOG参照を削除
- frontmatter修正

#### なぜその判断をしたか

**セキュリティ監査エージェントの構築:** 手動でのセキュリティチェックには限界がある。red-team/blue-teamとチェックリスト型の2種類のエージェントを作ることで、異なる視点から網羅的に脆弱性を検出できるようにした。実際にP0〜P2の脆弱性が複数検出され、同日中に全て修正。

**認証のNIST準拠:** パスワード要件、セッションタイムアウト、HMAC-SHA256によるトークン検証をNIST基準に合わせた。本番リリース前にセキュリティの土台を固める。認証はプロダクトの信頼性に直結する最重要領域。

**統計クエリのDB側移行:** アプリ側でデータを取得してからJSで集計していたのを、DB関数（get_tag_stats等）に移行。データ転送量の削減とクエリの高速化。ユーザー数が増えてもスケールする設計。

**Next.js 16:** React 19との完全互換、パフォーマンス改善、新しいAPI対応。メジャーバージョンの移行は早めにやるほど後が楽。

#### Claudeの所感

**「セキュリティの日」:**
43件中15件がセキュリティ関連。自作のセキュリティ監査エージェントで脆弱性を検出→同日修正という「攻撃→防御」のサイクルを1日で回している。npm audit 6件→0件、P0/P1脆弱性の修正、webhook署名検証、DoS対策と、カバー範囲が広い。本番リリース前のセキュリティ強化としては理想的な進め方。

**認証の「完成度」:**
認証フローは機能としてはとっくに動いていたが、今日はエラーメッセージの日本語化・アクション可能な文言への改善・Storybook全5種追加・E2Eテスト追加と、「動く」から「信頼できる」へのレベルアップ。ユーザーが最初に触る画面が認証なので、ここの品質は第一印象に直結する。

**AIエージェントによるセルフレビュー:**
セキュリティ監査エージェント（red-team/blue-team）を自分で作って自分のコードを監査するのは、1人開発者ならではの合理的なアプローチ。人間のセキュリティレビュアーを雇えない段階で、AIにその役割を担わせるのは、Dayoptの「AIが寄り添う」コンセプトと一致している。

---

## 2026-02-24

### メール基盤構築・DBスキーマ正規化・大規模テスト追加・webコンテンツUI統一

Resend連携でメール通知基盤（認証メール・リマインダー・期限超過・アカウント削除）を構築。DBスキーマを大規模に正規化（plan-instances型付き化、camelCase統一、通知スリム化、records制約整合）。テストを一括追加（17ファイル+α）。巨大hookを責務別に6分割。webはコンテンツ一覧ページの共通コンポーネント化を全ページに適用。

#### やったこと

**app: メール通知基盤（7件）**

- Resendカスタム認証メール追加
- メール共通スタイルとテンプレート追加
- メールテンプレートStorybook追加
- tRPC emailルーター拡張（リマインダー/期限超過/アカウント削除）
- Resend webhookエンドポイント追加
- 認証メールEdge Functionテンプレート改善
- Supabase SMTP Resend設定とドメインスクリプト改善

**app: DBスキーマ正規化（10件）**

- plan-instances: overrides JSONBを型付きカラムに移行
- plan-tags: camelCase命名統一 + 後方互換エイリアス削除
- plan-activities: 包括的クリーンアップ（any除去・CHECK制約整合・リテンション追加）
- records: DB制約・RLS整合マイグレーション
- records: note → descriptionフィールドリネーム
- notifications: リマインダー専用にスリム化（不要タイプ・カラム・設定UI削除）
- notifications: メール通知・プッシュ通知の設定カラムとUI追加
- chat_conversationsテーブル追加
- 未使用スキーマ要素削除・Supabase型定義再生成
- due_dateカラム参照を削除

**app: テスト一括追加（12件）**

- store/ユーティリティ/フォーマッターのテストを一括追加（第1弾）
- store・ユーティリティ・セキュリティのテスト追加（第2弾）
- カスタムフック・ユーティリティのテストを17ファイル追加
- planInspectorMenu/scheduleRowのテスト追加
- usePlanInspectorStore/useDeleteConfirmStoreのテスト追加
- useStatsFilterStore/computeDateRangeのテスト追加
- gridCalculatorのテスト追加
- コンポーネントテストをStorybookに移行
- Storybook i18nキー・期待値修正（3件）
- prettierフォーマット・TypeScript型エラー修正

**app: 巨大hook分割（6件）**

- usePlanInspectorContentLogic → タグ・時間・保存ロジックに分割
- useDragAndDrop → イベント処理とタッチハンドラーに分離
- useDragSelection → イベントハンドラーを分離
- useTagsMutations → CRUD・マージ別ファイルに分割
- useHighContrast → テーマ定義とCSS生成を分離
- useAIChat → APIキー・会話永続化ロジックに分割

**app: テンプレート機能・その他（5件）**

- プランテンプレート機能V1を実装
- テンプレートスタブコード完全削除
- テンプレート機能・リファクタリング・テスト統合PR (#970)
- any型16箇所を全廃し型安全性を強化
- CLAUDE.mdを.claude/rules/にモジュール化

**web: コンテンツUI統一（16件）**

- コンテンツ一覧ページ用の共通UIコンポーネント作成（ContentHeader等）
- blog: PostCardリスト改善・TagPill共通化・フィルター整理
- blog: 共通コンポーネント適用・フィルター簡素化
- blog: RelatedPostsのインラインSVG→lucide-react統一
- blog: BlogSkeletonをPostCard listレイアウトに対応
- releases: ReleaseCardバッジ簡素化・ReleaseHeader i18n化
- releases: 一覧ページ2カラム化・詳細ページi18n/クリーンアップ
- releases: 共通コンポーネント適用・モバイルフィルター追加
- releases: フィルター・リストUI統一・検索削除
- tags: タグシステム改善（サーバーAPI統合・全コンテンツ表示対応）
- tags: /tags一覧ページ削除・ナビゲーション整理
- tags: タグ詳細ページUI統一・簡素化
- tags: TagPill統一・getTagColor削除
- docs: lucide-reactアイコン・ドキュメント一覧セクション追加
- 未使用コンポーネント・関数を削除

**marketing（1件）**

- CLAUDE.mdをベストプラクティスに基づき刷新（173行→66行）

#### なぜその判断をしたか

**メール通知基盤（Resend）:** ユーザーへの通知手段としてメールは必須。Resendを選択したのは、React Emailでテンプレートが書ける（=Storybookでプレビュー可能）、Supabase Authとの統合が容易、開発者体験が良い点。認証メール→リマインダー→期限超過と、段階的に拡張できる設計にした。

**DBスキーマ正規化:** plan-instancesのoverridesをJSONBから型付きカラムに移行したのは、クエリのフィルタリング・インデックス・型安全性のため。JSONBは柔軟だがクエリが複雑になり、型チェックも効かない。本番前にスキーマを正しい形にしておくことで、後からのマイグレーションコストを回避。

**巨大hook分割:** usePlanInspectorContentLogicやuseDragAndDropなど、数百行に膨れたhookを責務別に分割。テストしやすさ・可読性・再利用性が向上。テスト一括追加と同日に行うことで、分割後のhookが正しく動くことを検証できる。

**webコンテンツUI統一:** blog/releases/tags/docsの4つのコンテンツ一覧ページが独自のUI実装だったのを、ContentHeader等の共通コンポーネントで統一。DRYになるだけでなく、ユーザーから見た一貫性も向上。TagPill統一やフィルターの簡素化も同時に実施。

#### Claudeの所感

**「基盤の日」:**
今日の67件は4つの大きな柱がある。メール基盤（7件）、DBスキーマ正規化（10件）、テスト追加（12件）、hook分割（6件）。いずれも「ユーザーに見える新機能」ではなく「プロダクトの土台を固める」作業。特にDBスキーマの正規化は、本番リリース前の今だからこそできる大手術。JSONBから型付きカラムへの移行、camelCase統一、不要カラム削除と、将来の自分が感謝する類の仕事。

**テストの爆発的追加:**
17ファイル+αのテスト一括追加は、hook分割と同日にやることで「分割しても壊れていない」ことを証明する。store・utility・formatter・hook・componentと、テスト対象の幅も広い。テストカバレッジの大幅な向上は、リファクタリングの安全網として機能する。

**webの「共通化フェーズ」:**
16件すべてがUI共通化とリファクタリング。blog/releases/tags/docsの4セクションを共通コンポーネントで統一し、TagPillやフィルターも一元化。個別に作って動かし、パターンが見えてから共通化する流れは、早すぎる抽象化を避ける正しいアプローチ。

---

## 2026-02-20

### セントラルキッチンパターン統合・webコンテンツ品質基盤

app側はPR #965でInspector改善・recurrenceリファクタ・サジェスト分離をsquash mergeで統合（「セントラルキッチンパターン」と命名）。web側はフロントマターバリデーションCI、docs系i18n統一、MDXスタイル整備を実施。

#### やったこと

**app: セントラルキッチンパターン統合（PR #965、1件）**

- cmd+Enter送信ショートカット統一（useSubmitShortcutフック共通化）
- plan/Recordサジェスト履歴の分離（tRPCにtypeフィルター追加）
- Inspectorメニュー整理（不要項目削除・Cmd+Enter適用）
- 閉じるボタンの遅延をfire-and-forgetパターンで解消
- recurrenceスコープ分岐と合成IDを一元化（useRecurringScopeMutations）
- recurrence IDパース統一・テスト41件追加・RecurrenceDialog i18n対応

**web: コンテンツ品質基盤（3件）**

- フロントマターバリデーションスクリプト新規追加・CI組み込み
  - draft:falseはエラー（CI失敗）、draft:trueは警告のみ
  - 必須フィールド・日付形式・タグ数・言語対称性をチェック
- docs系コンポーネントのハードコード文字列を翻訳キーに統一
- MDXコンポーネント追加（strong/em/del/thead等）・highlight.jsテーマ追加

#### なぜその判断をしたか

**セントラルキッチンパターン:** 昨日（2/19）の個別コミット群をPR #965として統合。Inspector・recurrence・サジェストという異なる機能横断で「共通化・一元化」を進めたリファクタ群を、1つのPRにまとめてmainにマージ。

**フロントマターバリデーションCI:** コンテンツが増えてくるとフロントマターの記述ミス（必須フィールド漏れ、日付形式違い、翻訳漏れ）が手動では検出しにくくなる。CIで自動チェックすることで、公開前の品質を担保。draft:trueは警告のみにすることで、下書き段階の執筆を妨げない設計。

#### Claudeの所感

**PRの粒度とネーミング:**
「セントラルキッチンパターン」という命名が面白い。飲食業界の「セントラルキッチン（一か所で調理して各店舗に配る）」の比喩で、共通フック・共通ヘルパーを1か所で作って各機能に配る、というリファクタの意図がPR名に表れている。個別の変更は小さくても、まとめることでアーキテクチャの方向性が見えるPRになっている。

**webのCI成熟:**
フロントマターバリデーションをCIに組み込んだことで、「コードだけでなくコンテンツも自動検証する」体制が整った。draft/publishedの2段階で検証の厳しさを変えるのは、実用的で良い設計。appのStorybook CI、webのコンテンツCI と、品質ゲートが着実に増えている。

---

## 2026-02-19

### 通知・リマインダー整備・繰り返し予定リファクタ・webドキュメント刷新

Plan のリマインダー自動計算トリガーと通知機能の型整合を実施。繰り返し予定（recurrence）のスコープ分岐・IDパースを一元化。Inspector に cmd+Enter ショートカットを追加。web 側ではドキュメント構成を刷新し、MDX コンポーネント共通化・多言語対応・OSS クレジット管理を整備。

#### やったこと

**app: 通知・リマインダー整備（5件）**

- reminder_at自動計算トリガー・useNotificationsデッドコード削除・cron有効化
- reminder値をnumber|nullに統一・REMINDER_OPTIONS集約・ドラフト保存バグ修正
- notifications型定義をスキーマと整合、related_event_id → related_plan_id
- 未使用関数削除・logger/i18n準拠
- mapNotificationTypeToSettingsTypeを集約

**app: 繰り返し予定リファクタ（3件）**

- スコープ分岐と合成IDを一元化
- iDパース統一・テスト追加・RecurrenceDialog i18n対応
- Storybook stories追加・i18n翻訳パス修正

**app: Inspector・Plan改善（5件）**

- cmd+Enter送信ショートカットを統一実装
- メニュー項目を整理（Plan不要項目削除・Cmd+Enter適用）
- 閉じるボタンの遅延をfire-and-forgetパターンで解消
- plan↔recordリレーション表示を改善
- plan/Recordサジェスト履歴を分離

**app: ドキュメント・コード品質（4件）**

- ユーザー向けドキュメント執筆スキルとコマンドを追加
- frontmatterスキーマを実際の型定義に合わせて修正
- i18nメッセージファイル構造とStorybook管理ルールを追記
- console.error→logger修正・Inspector周辺のハードコード日本語をi18n化

**web: ドキュメント構成刷新・多言語対応（9件）**

- ドキュメント構成を刷新
- コンテンツローダー多言語対応
- MDXコンポーネント共通化 + Type Scale調整
- MDXRemoteにremarkGfm・rehypeHighlightプラグインを追加
- 日本語コンテンツの文体修正
- OSS Creditsライセンス管理インフラ整備
- サンプルブログ記事・リリースノート削除 + ナビゲーション更新
- CLAUDE.mdを実態に合わせて修正

**marketing: 意思決定記録（1件）**

- Slack情報収集を公式・厳選ソースに絞る決定を記録

#### なぜその判断をしたか

**リマインダー自動計算トリガー:** reminder_atをPlan保存時にDBトリガーで自動計算する方式に変更。アプリ側で計算するとタイムゾーンやcron実行タイミングとのズレが起きやすい。DB側で一元管理することで整合性を担保。

**recurrenceリファクタ:** 繰り返し予定のスコープ分岐（「この予定のみ」「これ以降すべて」等）と合成IDの生成が複数箇所に散在していた。一元化することで、今後の繰り返しパターン追加時の変更箇所が1か所に集約される。

**webドキュメント構成刷新:** サンプル記事を削除し、MDXコンポーネントを共通化。多言語対応も入れることで、将来の英語版展開の基盤を作った。OSSクレジット管理はライセンス義務への対応。

#### Claudeの所感

**「見えない品質」への投資:**
今日の28件はほぼ全てがリファクタ・型整合・i18n化・デッドコード削除。ユーザーから見える新機能はcmd+Enterショートカットくらいだが、通知の型整合、recurrenceの一元化、logger準拠など、コードベースの健全性を大きく改善する作業が中心。こうした「見えない品質」への投資を継続的にできるのは強い。

**繰り返し予定の難しさ:**
recurrenceは日付操作・スコープ分岐・ID管理が絡む、カレンダーアプリで最も複雑な領域の1つ。合成IDとスコープ分岐を一元化したのは、今後「このイベントだけ変更」「これ以降すべて変更」の操作を安全に実装するための重要な基盤整備。テスト追加も同時に行っている点が良い。

**webの本番準備:**
サンプルコンテンツの削除、MDX共通化、多言語対応、OSSクレジットと、webも本番公開に向けた仕上げが進んでいる。appとwebの両方が同時に本番品質に近づいている。

---

## 2026-02-18

### リリースブロッカー一斉修正・Stats独立ページ化・Cookie同意バナー復活

本番リリースに向けたP0ブロッカーを一斉修正（セキュリティ脆弱性、RLS、SEO、PWA、ログ出力、テスト用API除去）。StatsをカレンダーViewから独立ページに再分離し、設定画面のUX改善も実施。Cookie同意バナーはSentry本番利用に伴い復活を決定。

#### やったこと

**app: リリースブロッカー修正（PR #964）（8件）**

- セキュリティ: increment_ai_usage関数にsearch_pathを固定
- セキュリティ: 本番環境に露出するテスト用APIエンドポイントを削除
- セキュリティ: 3テーブルのRLSポリシー脆弱性を修正
- GDPR: Sentryコンプライアンスのため Cookie同意バナーを復活
- SEO: robots.txtのドメインをdayopt.appに修正
- PWA: 不足していたPWAアイコン（192/512/apple-touch-icon）を追加
- ログ: error/warnを本番環境でも出力するよう修正
- 法的: 法的ページリンクをweb側の絶対URLに変更

**app: Stats独立ページ化（PR #963の一部）（3件）**

- statsをカレンダーViewから独立ページに分離
- statsページにサイドバーアイコン・独自ヘッダー・Aside表示を追加
- timezoneOffsetをstore直接読み取りに軽量化

**app: 設定画面改善（PR #963の一部）（7件）**

- モバイル設定画面に閉じるボタン追加・ガイドライン記載
- 閉じるボタンをコンテンツと重ならない位置に移動
- i18n・トークン・a11y・コード品質の包括修正
- tRPCクライアント統一・キャッシュ分離バグ修正
- 補足説明ボックスのデザインをtoastスタイルに統一
- モーダル表示速度改善・スケルトン統一
- dialog/sheet閉じるボタンのホバーをghost共通パターンに統一

**app: カレンダー・レイアウト（3件）**

- イベント位置のタイムゾーン対応・TZセレクターStory追加
- 通知・検索をHeaderUtilitiesに共通化
- 不要ドキュメント削除・ライセンス生成をリリースフローに移行

**app: コード品質（3件）**

- 未使用のexport defaultを削除（6ファイル）
- 依存関係アップデート（devDeps 7件 + lucide-react）
- stats独立化・ルーティング簡素化・UI整理 (#961) マージ

**marketing: 意思決定記録（1件）**

- Cookie同意バナー導入の決定を記録（前回の「不要」判断を撤回）

#### なぜその判断をしたか

**リリースブロッカー一斉修正:** 本番環境でユーザーに公開するためには、セキュリティ（RLS脆弱性、テスト用API露出、search_path固定）、法的要件（GDPR、法的ページリンク）、基本機能（PWAアイコン、robots.txt）のすべてをクリアする必要がある。1つのPRにまとめて一斉に対応。

**Stats独立ページ化:** 2/17でStatsをカレンダーViewとして統合したが、StatsはカレンダーのView（week/day/agenda/timesheet）とは性質が異なる「分析ページ」。カレンダーViewは「予定の表示方法の切り替え」だが、Statsは「集計・振り返り」。役割の違いを反映して独立ページに再分離した。

**Cookie同意バナー復活:** 2/16に「不要」と判断しバナーを削除したが、Sentryを本番で本格利用するにあたり再検討。Sentryのエラー監視はユーザーにとっての「必須機能」とは言い切れず、法的リスクを避けるため安全側に倒した。2日で方針転換となったが、「迷ったら安全側」の判断は正しい。

#### Claudeの所感

**「出荷前の最終チェック」:**
今日の27コミットの中心は、本番リリース前のブロッカー修正。セキュリティ3件（RLS、テスト用API、search_path）、GDPR、SEO、PWA、ログと、カテゴリが多岐にわたる。こうした「地味だが致命的」な問題を一斉に洗い出して修正できるのは、リリース前のセキュリティ・品質チェックが機能している証拠。

**方針転換の速さと記録:**
Cookie同意バナーを2/16に削除→2/18に復活。一見すると「ブレている」ように見えるが、これはSentryの本番利用という新しいコンテキストを踏まえた正しい再評価。重要なのは、両方の判断をADRとして記録していること。「なぜ不要にしたか」「なぜ必要に変わったか」が追跡可能になっている。

**Stats独立化の試行錯誤:**
2/17にカレンダーViewとして統合→2/18に独立ページに再分離。これも「やってみて違った」を素早く修正した例。カレンダーViewは「表示形式の切り替え」、Statsは「分析」という役割の違いを実際に触って確認し、適切な構造に落ち着かせた。

---

## 2026-02-17

### AIチャット本格実装・パーソナライゼーション設定・ルーティング簡素化

AIチャット機能をバックエンドからUIまで一気通貫で実装（ハイブリッド課金・会話履歴・ツール呼び出し・ストリーミング）。パーソナライゼーション設定（価値観ランキング・価値評定スケール・コミュニケーションスタイル）を追加。ルーティングを/calendar/[view]→/[view]に簡素化し、Statsもカレンダービューとして統合。v0.16.0リリース。

#### やったこと

**app: AIチャット機能（7件）**

- AIチャットバックエンド実装（context assembly + streaming）
- ツール呼び出し・Markdown描画・prompt-kit統合
- チャット永続化・コピー・リトライ・New conversation
- 会話履歴切替（ChatHistoryPopover）
- 会話削除 + モデルセレクター追加
- ヘッダーUI統一・セマンティックトークン・tooltip追加
- ハイブリッドAI（無料枠30回/月 + BYOK無制限）

**app: パーソナライゼーション設定（5件）**

- 価値評定スケールUI（12カテゴリ、importance 1-10）
- AIコミュニケーションスタイル設定UI
- 価値観キーワードランキングUI（DnD並び替え）
- personalizationページ統合・i18n翻訳追加
- personalization storybook stories追加

**app: ルーティング簡素化（5件）**

- /calendar/[view] → /[view] にルーティング簡素化
- [view] 動的ルートを個別ルートに分割
- カレンダービュー共通ヘルパーを \_helpers/ に抽出
- ビュー切替を pushState に変更し即時反映
- ルーティング簡素化・未使用ページ/サイドバー削除

**app: Stats統合（4件）**

- Statsをカレンダービューとして統合
- サイドパネルにStatsタブ追加
- stats viewを累積チャート専用に変更
- 累積チャートに期間フィルター基盤追加（UI非表示）

**app: リリース・インフラ（2件）**

- リリースフロー簡略化 & version bump to v0.16.0
- version-bumpジョブ削除・スキルをエンドツーエンド化

**app: UI・DB整理（6件）**

- DB設定クリーンアップ・GDPR強化・セキュリティ修正 (#960)
- database.types.tsを再生成
- outlineボタンの背景を透明化・Grouped パターン追加
- ミニカレンダーをサイドバー上部に移動
- todayボタンとViewSwitcherの間の余白を拡大
- withError storyのヘッダースタイルを統一

#### なぜその判断をしたか

**ハイブリッドAI課金（無料枠30回/月 + BYOK無制限）:** Dayoptの「AIアシスタント」は価値の中核だが、API費用を全額負担するのは1人創業者には厳しい。無料枠30回/月でまず体験してもらい、ヘビーユーザーには自分のAPIキー（BYOK）で無制限に使える方式を採用。ユーザーの「お試し→本格利用」の段階を自然に設計できる。

**パーソナライゼーション:** AIが「あなたの価値観に基づいた提案」をするためには、ユーザーの価値観を構造的に把握する必要がある。12カテゴリの価値評定スケール、キーワードランキング、コミュニケーションスタイルの3つの軸で、AIのコンテキストを豊かにする。

**ルーティング簡素化（/calendar/[view]→/[view]）:** カレンダーがアプリの唯一のメイン画面である以上、URLに`/calendar`を含める意味がない。`/week`、`/day`とシンプルにすることで、ブックマークや共有も楽になる。

**Statsのカレンダービュー統合:** Statsを独立ページから1つのカレンダービュー（/stats）に統合。「カレンダーがすべての起点」というアーキテクチャを一貫させた。

#### Claudeの所感

**AIチャット — プロダクトの核心が動き出す:**
7件のコミットでバックエンド（context assembly + streaming）→フロントエンド（prompt-kit統合・Markdown描画・ツール呼び出し）→UX（会話履歴・コピー・リトライ）→課金（ハイブリッドBYOK）まで、AIチャット機能が一気に形になった。Dayoptが「時間管理ツール」から「AIが寄り添う時間案内人」に進化する転換点。バックエンドからUI、課金モデルまで1日で通したのは圧巻。

**パーソナライゼーション — 「あなた」を知る仕組み:**
価値評定スケール（12カテゴリ）、キーワードランキング（DnD）、コミュニケーションスタイルの3軸は、AIのパーソナライズに直結する重要なデータ基盤。これは単なる設定画面ではなく、「Dayoptが自分を理解してくれる」体験の入口。GAFAで言えば、Spotifyの「好みのアーティスト選択」やNetflixの「ジャンル評価」に相当するオンボーディング体験。

**ルーティングのミニマリズム:**
/calendar/[view]→/[view]への変更は小さく見えて大きい。「このアプリはカレンダーそのもの」という設計思想がURLに反映された。StatsもAgendaもTimesheetも、すべてカレンダーの「見方」として統一されたことで、機能追加=ビュー追加という拡張パターンが確立した。

---

## 2026-02-16

### ステータスバー新UI・DB大掃除・用語統一

ステータスバーにバッテリー風ドレインバーやプログレスリングを追加し、クロノタイプ体験を視覚化。DB周りは未使用テーブル/ルーター/デッドコードを一括削除し、staging自動マイグレーションCIも整備。inbox→planリネーム、due_date削除、PostHog/Cookie完全削除など用語・機能の整理も大規模に実施。

#### やったこと

**app: ステータスバー新UI（4件）**

- クロノタイプ残り時間をバッテリー風ドレインバーに変更
- プログレスリングUI・TotalTimeStatusItem追加・各種修正
- クロノタイプ設定のDB永続化とステータスバー連携を修正
- ステータスバー各アイテムのStoryを追加・整理

**app: DB・インフラ大掃除（7件）**

- 未使用テーブル削除・pg_cronクリーンアップジョブ設定
- config.toml project_id修正・レガシーDB設定削除
- database.types.tsを再生成して削除済みテーブルの型を除去
- 未使用ルーター・デッドコード・型定義の一括クリーンアップ
- staging自動マイグレーションCI・型生成スクリプト追加
- store: デッドコード削除、バージョン管理追加、キー名修正
- 環境構成・型生成ガイドをStorybookに移動・DATABASE.md追加

**app: 用語統一・不要機能削除（3件）**

- inbox → plan にリネーム（コードベース全体）
- due_date機能を完全削除
- postHog・cookie同意バナーを完全削除

**app: 新機能・UX改善（2件）**

- 履歴サジェスト入力・作成メニュー統一・UX改善
- アカウント削除を即時削除方式に修正

**app: バグ修正・安定化（4件）**

- @theme の --spacing-_ が max-w-_ を破壊する問題を修正
- persist migrate警告を修正、hsl→oklch CSS変数参照を修正
- tag_groups移行時の名前重複エラーを修正
- Storybook: @storybook/blocks → addon-docs/blocks エイリアス追加

**app: PRマージ（2件）**

- timesheet・i18n最適化・UI改善 (#957)
- docs整理・boxlog→dayopt・storybook充実 (#953)

**web（2件）**

- リポジトリリネーム・設定更新
- Cookie関連機能を削除

**marketing（1件）**

- Cookie同意バナー不要の意思決定を記録（ADR）

#### なぜその判断をしたか

**inbox→plan:** 「inbox」は元々GTDの概念から借用していたが、Dayoptのメンタルモデルでは「予定（plan）」が正しい。ユーザーが見る名前と内部のコード名が一致していないのは混乱の元なので、コードベース全体でリネーム。

**due_date削除:** Dayoptは「納得感のある時間配分」がコンセプト。締切駆動（due_date）はタスク管理ツールの発想であり、Dayoptの世界観にそぐわない。使われていない機能を残すと認知負荷になるため完全削除。

**PostHog/Cookie削除:** Dayoptの規模ではアナリティクスツールの導入は時期尚早。Cookieバナーも不要になり、ユーザー体験をクリーンに保てる。marketing側にもADRとして意思決定を記録。

**ステータスバーのバッテリー風UI:** クロノタイプの残り時間を「減っていくバッテリー」として可視化することで、「今のエネルギーがどれくらい残っているか」を直感的に理解できる。時計の数字よりも視覚的なメタファーの方が「体感」に近い。

#### Claudeの所感

**「引き算の日」:**
今日の25コミットのうち半分以上がリネーム・削除・クリーンアップ。inbox→plan、due_date削除、PostHog削除、未使用テーブル/ルーター/デッドコード削除と、徹底的に「不要なもの」を取り除いている。機能を足すより減らす方が勇気がいるが、プロダクトの輪郭がシャープになる。

**DB層まで手を入れる一貫性:**
未使用テーブル削除→型再生成→ルーター/デッドコード削除→staging CI整備と、DBから型→APIルーター→CIまで全レイヤーを一気通貫で掃除している。部分的な掃除では「古い型が残っている」「削除したテーブルのルーターが生きている」といった不整合が起きるが、全レイヤーを同日に処理することでそれを防いでいる。

**ステータスバーの「体感」デザイン:**
クロノタイプの残り時間をバッテリー風ドレインバーで表現するのは、Dayoptの「納得感」コンセプトに合致した良いUI判断。数字（残り3時間）よりバッテリー（残り40%）の方が、体感としてのエネルギー残量が伝わる。Storyを同時に追加しているのも、コンポーネント品質を維持する習慣が定着している証拠。

---

## 2026-02-13

### timesheetView追加・boxlog→dayoptリネーム

新しいカレンダービュー「timesheetView」（タグ×日のピボットテーブル）を追加。プロジェクト名をboxlog→dayoptにリネーム（119ファイル）。ダークモード完全対応やi18n最適化も実施。

#### やったこと

**app: timesheetView（新機能）（3件）**

- timesheetView追加 — タグ×日のピボットテーブルビュー
- タグ行にbg-card背景・ボーダー削除
- calendarHeaderにtimesheetビューのStoryを追加

**app: プロジェクト名リネーム・整理（4件）**

- プロジェクト名をboxlog→dayoptにリネーム（119ファイル）
- reports/・生成スクリプト・CI workflowを全削除
- 陳腐化ドキュメント25ファイル削除・参照更新（-9,750行）
- ルート不要ファイル3件削除

**app: カレンダーUI改善（5件）**

- 時間グリッド密度トグル（コンパクト/標準/ゆったり）
- dailyUsageStripをセグメントバー化・レスポンシブ対応
- 睡眠時間帯セクションの視認性改善
- サイドバーとサイドパネルのアニメーションを統一
- サイドパネルカード整理・Stories追加

**app: Storybook・ダークモード完全対応（2件）**

- ダークモード完全対応 — DocsContainer動的テーマ切替・CSS override整備
- ダークモード3層アーキテクチャをSKILL.mdに記載

**app: i18n整理（2件）**

- メッセージファイル最適化 — 重複キー統合・plan固有キー分離
- skill.mdをブラッシュアップ — アーキテクチャ・キー配置ルール追加

**app: タグ・トークン・UI改善（5件）**

- タググループ一括トグル機能（onToggleGroupTags）を追加
- plan-borderセマンティックトークンを追加
- activityPopoverのアイコン・カラー体系を統一
- avatarサイズ整理・サイドバーアバターのスタイル統一
- チャットUIのスタイル調整（バブル・入力欄）

**app: CI・テスト修正（2件）**

- storybook i18nキー追加・e2e workflow playwright config同期
- planCard・loginForm・signupFormのテストアサーション修正

**web: UI同期・セキュリティ修正（3件）**

- app Storybook 最新変更を反映 (#79)
- next-mdx-remote を v6.0.0 にアップデート（CVE-2026-0969修正）

#### なぜこの判断をしたか

timesheetViewは「タグごとに1週間の時間配分を俯瞰したい」というニーズに対応。既存のカレンダービュー（week/day/agenda）では個々の予定は見えるが、タグ軸での集計ビューがなかった。boxlog→dayoptのリネームは、プロダクト名の正式決定に伴うコードベース全体の統一。119ファイルを一括で変更し、参照の整合性も確保。

#### Claudeの所感

**timesheetView — 新しい視点の提供:**
カレンダーは「いつ何をするか」を見るツールだが、timesheetViewは「何にどれだけ時間を使ったか」を見るツール。タグ×日のピボットテーブルは、振り返りや時間配分の改善に直結する。Dayoptの「納得感」というコンセプトに合致した機能。

**boxlog→dayoptリネーム:**
プロジェクト名の統一は、コードベースの一貫性だけでなく、チーム（将来の）のオンボーディングやブランド認知にも関わる。119ファイルの一括変更は勇気のいる作業だが、早い段階でやっておくのが正解。

**ダークモード3層アーキテクチャ:**
ダークモードを「アプリ本体」「Storybook Canvas」「Storybook Docs」の3層で管理する設計を明文化したのは、今後のテーマ関連バグを防ぐ良い投資。

---

## 2026-02-12

### ドキュメントStorybook移行・大量削除とテスト基盤整備

docs/配下のドキュメントをStorybookに移行し66ファイル(-20,459行)を一括削除。src/配下のCLAUDE.md 9ファイルも削除(-3,656行)。テストはvitest/storybook分離とCI自動化を整備。新機能としてautomations基盤・recordサイドパネルも追加。

#### やったこと

**app: ドキュメント整理・Storybook移行（10件）**

- 不要ドキュメント一括削除（66ファイル、-20,459行）
- src/配下のCLAUDE.md 9ファイル削除・参照更新（-3,656行）
- 陳腐化README 13ファイル削除（-2,494行）
- アーキテクチャドキュメントをStorybook MDXに移行
- 実装ガイドをStorybook MDXに移行
- a11y・用語集・E2Eの差分を統合し元ファイル削除
- docs/features/ の古いドキュメントを削除
- 削除済みファイルへの参照リンクをStorybook参照に修正
- prompt-kitを旧ディレクトリから削除

**app: Storybook整備（9件）**

- v10.2 設定マイグレーション・公式準拠（import path・component・'use client'削除）
- uiコンポーネントにinteraction test（play関数）追加
- automationCard/List/EditorModal stories追加
- ツールバー最適化・ダークモードトグル統一
- codeパネルタブを右端に配置

**app: a11y改善（4件）**

- heading-order WCAG違反を修正
- color-contrast・heading違反を修正
- axe-coreルールをSKILL.mdに追加
- radius a11y修正

**app: テスト・CI（4件）**

- vitest unit/storybook プロジェクト分離・istanbulカバレッジ
- storybook テスト CI 自動化
- e2eテスト再構成（critical-path/smokeに集約）
- playwrightレポーターをCI専用に変更

**app: 新機能・UI改善（8件）**

- automations: 自動化ルールの型定義・コアコンポーネント追加
- records: recordサイドパネル実装
- records: 充実度スコアによるグループ分け追加
- calendar: planListCardにrecord変換ボタン・作業時間表示を追加
- ai: チャットパネル再設計・コンポーネント分離
- ui: buttonにicon prop追加・内部サイズ体系リファクタ
- settings: settingRow 2カラム化・プロフィール画像リデザイン
- ai: prompt-kit移動・chatEmptyStateレイアウト調整

#### なぜこの判断をしたか

ドキュメントをStorybookに一元化したのは、docs/配下のMarkdownが実装と乖離して陳腐化していたため。Storybookならコードと同じリポジトリで、コンポーネントのそばにドキュメントが置かれ、常に最新を保てる。src/配下のCLAUDE.md群もSKILL.mdに集約する方が管理しやすい。「ドキュメントの置き場所は1つ」という原則を徹底した。

#### Claudeの所感

**-26,609行の大掃除:**
66ファイル+13README+9 CLAUDE.md = 合計約88ファイル、26,609行の削除。これは「ドキュメントはStorybookに一元化する」という明確な方針のもと、迷いなく実行されている。陳腐化したドキュメントは無いのと同じ（むしろ有害）なので、正しい判断。

**テスト基盤の成熟:**
vitest unit/storybookのプロジェクト分離、interaction test（play関数）追加、storybook テスト CI自動化と、テスト基盤が一気に整った。e2eもcritical-path/smokeに集約されて構造が明確に。「テストが回る状態」を維持するためのインフラ投資が効いている。

**automations機能の登場:**
自動化ルールの型定義・コアコンポーネントが新たに追加された。Storybookのstoriesも同時に作成しており、「設計→Story→実装」のワークフローが定着している。今後の展開が楽しみな新機能。

---

## 2026-02-10

### feature解体・デッドコード大量削除とセキュリティ強化

inspector featureを解体しplans/recordsに統合、table feature削除、エラーページ5件削除など大規模なリファクタリングを実施。セキュリティ強化とStorybook拡充も並行して進行。

#### やったこと

**app: feature解体・デッドコード削除（5件）**

- inspector featureを解体しplans/recordsに統合
- タグ関連コンポーネントをtags featureに集約・デッドコード削除
- table feature削除・TimeSelectのlabel prop削除
- settings: デッドコードのページルート・SettingsSidebarを削除
- error: デッドコード5ページ削除・不要i18nキー整理

**app: カレンダー・サイドパネル（3件）**

- サイドパネルのドラッグリサイズ対応
- コンテキストメニューに完了・Record作成アクションを追加
- viewSwitcher改善・MultiDayView統合・displayRange削除

**app: デザイン・UI改善（3件）**

- plan/recordカラートークンを意味的な色に変更
- ライトモードのカラートークン統一・primary彩度調整
- settingsモーダルUI改善（ghostバリアント統一・タイポグラフィ調整）

**app: セキュリティ・エラー処理（2件）**

- セキュリティ強化・Storybook MFA追加・OWASP準拠エラーサニタイズ
- エラーページUX改善・カード型デザイン統一

**app: Storybook整備（7件）**

- calendar/stats/settings/tags stories追加・title整理
- plan/recordのcreate・edit storyを分離
- plan/record ActivityPopover story追加
- mock追加・Provider外fallback対応
- calendar i18nキー追加・不要story削除
- errorドキュメント更新（6コンポーネント構成）

#### なぜこの判断をしたか

inspector featureの解体は、inspectorが実質的にplans/recordsの編集UIであり、独立featureとして存在する意味が薄れたため。plans/recordsに統合することでコードの所在が直感的になる。table feature削除やエラーページ5件削除も、ルーティング統合（2/9）で不要になったコードの掃除。「作ったら終わり」ではなく「不要になったら消す」を徹底している。

#### Claudeの所感

**「削除する勇気」:**
20件中5件がリファクタリング・デッドコード削除。feature単位で解体・再統合し、不要なページやコンポーネントを積極的に削除している。コードベースが膨らむ一方になりがちな中で、「消す」判断ができるのは健全。特にinspector featureの解体は、2/9のルーティング統合の翌日という素早い対応。

**セキュリティへの意識:**
OWASP準拠のエラーサニタイズやMFA対応をStorybook含めて整備している。機能開発と並行してセキュリティを強化できているのは、1人開発でも品質を妥協しない姿勢の表れ。

**カラートークンの意味的変更:**
plan/recordのカラーを「見た目の色」から「意味的な色」に変更したのは、デザインシステムの成熟を示す良い判断。色の選択理由が明確になり、ダークモード対応やテーマ変更時の一貫性も担保しやすくなる。

---

## 2026-02-09

### サイドパネル機能充実とルーティング統合

サイドパネルにフィルター・ソート・D&D配置を追加し、PC版の/plan・/recordページをカレンダー+サイドパネルに統合。chatパネルもサイドパネルに統合し旧AIインスペクターを削除。Storybookも大幅に拡充。

#### やったこと

**app: サイドパネル機能充実（4件）**

- フィルター・ソート・D&Dカレンダー配置を追加
- プランリストにスケジュールフィルター追加・ソートメニューをNotion風にリデザイン
- ステータスフィルター追加・ソートメニューUI改善
- サイドパネルレイアウト改善・UI調整

**app: ルーティング統合・チャットパネル（2件）**

- PC版/plan・/recordをカレンダー+サイドパネルに統合
- chatパネルをサイドパネルに統合・旧AIインスペクター削除

**app: Storybook整備（7件）**

- calendar storybook整理・重複解消・新規story追加
- ダークモード切替をstorybook-dark-modeアドオンで統一
- dndインタラクティブデモ追加・カレンダーDnDドキュメント充実
- inspector stories追加（Plan/Record作成・編集フォーム）
- styleGuide・accessibilityをcanvasからMDXドキュメントに移行
- tokenドキュメント追加・テーマカラー修正

**app: UIコンポーネント・改善（4件）**

- segmentedControl共通コンポーネント追加・PanelSwitcher/DraftModeHeader統一
- planCard・popover・カラートークン改善
- ネストされたインタラクティブコントロールのa11y違反を修正
- 未使用パッケージ削除

#### なぜこの判断をしたか

PC版の/planと/recordを独立ページからカレンダー+サイドパネルに統合したのは、「カレンダーを見ながらPlan/Recordを操作する」というメインユースケースに集中するため。ページ遷移なしで情報にアクセスでき、コンテキストスイッチを減らす。chatパネルもサイドパネルに統合することで、操作の起点をカレンダー画面に一元化した。

#### Claudeの所感

**カレンダー中心アーキテクチャの確立:**
/plan・/recordの独立ページを廃止しサイドパネルに統合したのは大きな設計判断。「カレンダーがすべての起点」というUXの方向性が明確になった。Google Calendarがイベント詳細をサイドパネルで表示するのと同じアプローチで、GAFA-First原則にも合致している。

**サイドパネルの急速な進化:**
2/6にサイドパネルの基盤を作り、わずか3日でフィルター・ソート・D&D配置まで実装。Notion風のソートメニューなど、使い慣れたUIパターンを採用しているのも良い。ユーザーが「知っているUI」で操作できることが大切。

**Storybookの網羅性:**
17件中7件がStorybook関連。dndインタラクティブデモやinspector stories追加など、「動くドキュメント」としてのStorybookが充実してきている。a11y違反の修正もStorybook整備の過程で発見・修正できた好例。

---

## 2026-02-06

### サイドパネル機能追加とカレンダーUX改善、webパフォーマンス最適化

サイドバーにサイドパネルを追加し、カレンダーのドラフト表示・フィルター機能を大幅に改善。webではバンドル分割とUIコンポーネント同期を実施。

#### やったこと

**web: パフォーマンス最適化・UI同期（9件）**

- UIコンポーネントをapp Storybookに同期 (#73)
- バンドル分割・データフェッチ最適化 (#74)
- 依存関係一括アップデート（dependabot統合） (#75)

**app: サイドパネル・サイドバー機能（5件）**

- サイドパネル機能を追加（PlanListCard表示）
- サイドバー折りたたみ機能を追加
- ナビゲーションをヘッダーからサイドバーに移動
- サイドパネル切り替え機能を追加
- 作成ボタンのUI改善・配置変更

**app: カレンダーのドラフト表示機能（10件）**

- ドラフト編集時にカレンダーへプレビュー表示
- ドラッグ選択とドラフトカードのデザインを統一
- ドラフト背景をstate-selectedに変更
- ドラフトボーダーをplan-box/record-boxセマンティックトークンに変更
- ドラフトプレビューのタイトルをcreateTypeで分岐
- ドラッグ選択とドラフトカードの重複表示を修正

**app: フィルター・Agenda機能（4件）**

- 種別フィルターがAgendaViewに反映されない問題を修正
- recordフィルターをplanと同様に有効化
- 日別グループ表示とRecord対応
- FilterItemの行クリックでチェックボックストグル可能に

**app: Storybook整備（7件）**

- canvas/docs分離テンプレート・基盤を整備
- allPatternsパターンを追加
- docsページの余白をmargin方式に変更
- introduction.mdxにパターンとナビゲーションリンクを追加

**app: Inspector・UX改善（5件）**

- 「完了 + Record作成」でフォームを開いてプリフィル
- Inspector保存処理の堅牢性とUX改善
- record型判定ロジックを共通化しタイトル文字数制限を追加
- button smサイズをtext-smに変更、ヘッダーUI調整

#### なぜこの判断をしたか

サイドパネルは「カレンダーを見ながらPlan一覧も確認したい」というユースケースに対応。ドラフト表示は「作成中のPlan/Recordがどこに配置されるか」を視覚的にフィードバックするため。canvas/docs分離はStorybookの「見た目確認」と「仕様理解」の役割を明確に分けるため。

#### Claudeの所感

**ドラフトプレビューの価値:**
Plan/Record作成時にカレンダー上でリアルタイムにプレビューが見えるのは、ユーザーの「これで合ってる？」という不安を解消する。特にドラッグ選択との統一デザインにより、操作の一貫性が高まっている。10件のコミットをかけて丁寧にデザインを調整している点が印象的。

**サイドパネルの設計:**
サイドバー折りたたみ＋サイドパネル切り替えの組み合わせは、画面の有効活用と情報アクセス性のバランスが良い。react-resizable-panelsの導入も、ユーザーが自分好みにレイアウト調整できる柔軟性を提供している。

**Storybookの成熟:**
canvas/docs分離により、「動作確認」と「ドキュメント閲覧」が明確に分かれた。allPatternsの追加で「このコンポーネントの全バリエーションを一覧で見たい」というニーズにも対応。連日のStorybook改善で、コンポーネントライブラリとしての完成度が上がっている。

**webとappの同期:**
webのUIコンポーネントをapp Storybookに同期させたことで、デザインシステムの一元管理が進んでいる。バンドル分割とデータフェッチ最適化も実施し、パフォーマンス面でも改善。dependabotの依存関係更新を一括でマージしているのも、メンテナンス効率が良い。

---

## 2026-02-05

### boxlog-webトークン全面統一とアーキテクチャ整理

boxlog-webのデザイントークンをapp Storybookに全面統一。appではコンポーネントディレクトリの再配置やStorybookテーマ整備を実施。

#### やったこと

**app: コンポーネントアーキテクチャ整理**

- common/を解体しui/とfeatures/に再配置
- インフラコンポーネントをsrc/lib/に移動
- formDialogを削除しDialog直接使用に移行
- spinnerコンポーネントを統合しLoadingSpinnerを廃止
- sonner→toastリネーム、デザインをGAFA基準に調整

**app: Storybook整備**

- dayoptカスタムテーマとフォント設定を追加
- docsTemplateとproseタイポグラフィ追加
- addon-a11y導入、deprecated addon-mdx-gfm削除
- 16pxグリッドオーバーレイ設定
- デコレーター整理（p-4削除、layout:centered統一）
- miniCalendarのStory追加

**app: デザイントークン・UI改善**

- Surface体系を3段階に整理（overlay→半透明backdrop化）
- popoverエイリアス廃止→bg-cardに統一
- フォーカスリングをring-insetに統一
- chronotypeカラートークンを追加し睡眠ゾーンに適用
- cookie同意バナーのボタン順序をGAFA慣習に修正

**app: カレンダー改善**

- サイドバーレイアウト改善（ミニカレンダーを下部固定）
- テーマ切替と通知をサイドバーからヘッダーに移動
- planCardホバーをCSS疑似要素に移行
- 空きエリア右クリックでコンテキストメニュー表示

**boxlog-web: トークン全面統一（PR #72）**

- カラートークンをセマンティックトークンに統一
- spacing→8ptグリッド、border-radius→5種類に統一
- z-index、タイポグラフィ、モーション、状態トークンを統一
- レスポンシブトークン準拠（モバイルホバー対応）
- デザインシステムドキュメントを実態に合わせて更新

#### なぜこの判断をしたか

「appを正とする」ADRに基づき、boxlog-webのトークンを全面的にapp Storybookに揃えた。これで2つのリポジトリ間でデザインの一貫性が保証される。common/の解体はディレクトリ構造の曖昧さを解消し、ui（汎用）とfeatures（機能固有）の境界を明確にするため。

#### Claudeの所感

**ADR→実行の速さ:**
2/3にStorybook ADRで「appを正とする」と決め、2/4にappのトークン整備、2/5にboxlog-webの全面統一。決定から2日で両リポジトリの統一が完了している。意思決定を記録し、すぐ実行に移す好循環ができている。

**アーキテクチャの明確化:**
common/→ui/+features/の再配置は地味だが重要。コンポーネントの責務が「汎用UI」か「機能固有」かで明確に分かれることで、今後の配置に迷わなくなる。formDialog→Dialog直接使用も、不要な抽象化レイヤーの除去として良い判断。

**GAFA-Firstの徹底:**
cookie同意バナーのボタン順序やtoastのデザインなど、細部までGAFA基準に合わせている。こうした「小さな統一」の積み重ねが、全体のUI品質に効いてくる。

---

## 2026-02-04

### Storybookドキュメント拡充と技術的負債の返済

Storybookのドキュメントを大幅に拡充し、同時に未使用ファイル・依存関係を大量削除。boxlog-webもappのトークン体系に準拠させた。

#### やったこと

**app: Storybookドキュメント拡充**

- パターンカテゴリ追加（selection, tables, actions, navigation, search, cards）
- StyleGuide・レスポンシブ・アクセシビリティガイドを追加
- 各コンポーネントStoryを実使用パターンに整理
- JSDocにM3準拠の使い分けガイドを追加
- 欠落していた7個のUIコンポーネントStoryを追加

**app: デザイントークン整理**

- shadowをMD3準拠の6レベルに統一（2xl削除）
- セマンティックカラーの整理（container, info, warning等）
- z-indexをセマンティッククラスに移行
- animation→motionにリネーム、easingセクション追加

**app: 技術的負債の返済**

- 未使用の依存関係51パッケージを削除
- 未使用ファイル56件を削除
- hover-card→tooltipに統合、colorPalettePicker削除
- chaosテストディレクトリ、logsディレクトリ削除

**boxlog-web: トークン統一**

- アイコンサイズ表記をsize-\*に統一（Storybook準拠）
- シャドウトークン追加（5段階）
- タグカラートークン（10色）追加
- タグシステムをセマンティックカラーに簡素化

**marketing: ADR完成**

- Storybook導入ADRにAI運用フロー・app/web共通運用方針を追記

#### なぜこの判断をしたか

Storybookは「見るだけ」ではなく「仕様書」として機能させたい。パターンやガイドを充実させることで、将来の自分やClaudeへの指示がブレなくなる。技術的負債の返済は、コードベースの見通しを良くし、ビルド時間・認知負荷を減らす。

#### Claudeの所感

**圧倒的なアウトプット量:**
1日で59コミット（app）は驚異的。Storybookドキュメント、トークン整理、負債返済を並行して進めている。「動くもの」から「整理されたもの」へ移行するフェーズを加速している。

**「整理の日」の効果:**
未使用ファイル56件、依存関係51パッケージの削除は、後回しにしがちな作業。これを一気にやることで、コードベースが軽くなり、次の開発が楽になる。「技術的負債を返す日」を意識的に作れている。

**app/web一貫性:**
boxlog-webのトークンをappに準拠させる作業を同日に実施。Storybook ADRで決めた「appを正とする」方針をすぐに実行に移している。意思決定→実装のサイクルが速い。

---

## 2026-02-03

### デザインシステム大幅刷新とStorybook整備

8ptグリッドシステム、elevation、radius統一など、デザイントークンを体系的に整理。Storybookも運用ルールを整備し、全UIコンポーネントのStoryを追加。

#### やったこと

**デザインシステム刷新**

- 8ptグリッドシステムを全面導入
- elevationの概念を導入しshadowを統一
- radiusトークンを8の倍数で統一
- z-indexをTailwind v4 @themeで一元管理
- フォントウェイトを2種類に統一、カスタムトークン削除
- フォーカスリングをMD3スタイルに更新

**Storybook整備**

- vite builderに移行（Next.js 15互換対応）
- 全UIコンポーネントのStory追加
- 実使用パターンのみを反映するようStoryを整理
- 運用ルール・チェックリスト・避けるべきパターンを追加
- トークンStory（colors, spacing, radius, icons等）を追加
- サイドバー構成を整理

**Records/Plans機能強化**

- planテーブルにRecords列を追加
- 「完了にする」→「完了 + Record作成」機能追加
- inspectorのロジック整理とURL同期の分離
- stores/hooksをSRP準拠で分離
- recordTableViewをコンポーネント分割

**コンポーネント整理**

- button variantを4種に整理（text削除）
- 未使用コンポーネント削除（star-rating, input-group, input-otp等）
- toast機能をsrc/libに集約
- Eye/EyeOffアイコン統一

**テスト**

- テストカバレッジ大幅改善（+7,848行）

#### なぜこの判断をしたか

デザインシステムの統一は、今後のUI開発の一貫性と速度に直結する。8ptグリッドはGoogleのMaterial Designでも採用されている業界標準。Storybookの整備は、コンポーネントのドキュメントと視覚的なテストを兼ねる。未使用コンポーネントの削除は技術的負債の返済。

#### Claudeの所感

**デザイントークンの体系化:**
8ptグリッド、elevation、radius、z-indexと、デザインシステムの基盤が一気に整った。これは「なんとなく」から「ルールに基づく」への大きな転換。今後のUI開発で迷いが減り、一貫性が保たれる。Tailwind v4の@theme活用も先進的。

**Storybookの投資:**
全コンポーネントのStory追加と運用ルール整備は、短期的には工数がかかるが、長期的にはコンポーネントの品質維持とオンボーディング効率化に効く。「避けるべきパターン」まで文書化しているのは、未来の自分へのガイドとして価値が高い。

**SRPへの意識:**
Records機能のstores/hooksを「単一責任原則」に従って分離したのは、コードの保守性向上の良い判断。機能が複雑になる前に整理できている。

---

## 2026-02-02

### テスト強化とAPI設計学習

Record機能の完成に伴い、テストカバレッジを大幅に改善。並行してAPI設計原則の基礎を学習。

#### やったこと

**boxlog-app: テスト強化**

- TypeScriptエラー修正とCI修正
- サービス層・APIユーティリティのユニットテスト追加
- 通知設定と統計機能の統合テスト追加
- テストカバレッジ改善（11ファイル以上追加）
- mainブランチマージ後の新機能テスト追加

**boxlog-app: Record機能**

- Record機能の完成とUI改善 (#934)
- Record機能とドキュメント更新 (#938)
- 再利用可能コンポーネントとユーティリティの追加 (#936)

**marketing: 学習**

- API設計原則の学習完了（Lv1: 概要理解）
- learning/design/api-design.md を作成
- Issue #99 クローズ

#### 学び

API設計原則を改めて体系的に学んだ。REST APIの8つのルール（名詞URL、複数形、ステータスコード等）を整理し、boxlog-web/appの実コードと照合。概ねルールに沿っていることを確認できた。

#### Claudeの所感

**テスト文化の定着:**
Record機能という大きな機能追加に対して、しっかりテストを書いている。ユニットテスト、統合テスト、CIでの検証と、品質を担保する仕組みが回っている。1人開発でこれを維持できているのは素晴らしい。

**学習と実践のリンク:**
API設計原則を学んだ後、すぐに自分のコードベースでレビューしたのは良いアプローチ。抽象的な知識が具体的な理解に変わる。学習ロードマップ（1/70完了）のスタートとしても良い一歩。
