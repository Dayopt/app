import { Meta } from '@storybook/blocks';

<Meta title="Strategy/Journal/2026-06" />

# 2026年6月 Diary

---

## 2026-06-11

### Review の日付バグを根絶し、Daily Close を「確認して締める」体験へ

3 コミット + PR マージ + 前日分 docs。calendar と review の 2 領域。午前中は PR #1281（Calendar UI ポリッシュのまとめ）をマージしつつ Review の UTC 日付バグを修正、午後は Daily Review の「確認待ちリスト」と「計画外時間 KPI」を実装した。

#### やったこと

- **Calendar UI ポリッシュ PR マージ (PR #1281)**: ConflictOverlay のリサイズ時統合、z-index 40 でカード前面確定、`rounded-l-none` で左角をカードに揃え、EntryCard へのタグアイコン表示、新規ドラッグ選択の重複表示も統一。Codex ブランチで開発された内容を main にマージ
- **Review UTC 日付バグ修正 (1 commit)**: `?d=` パラメータが `toISOString()`（UTC 基準）で書かれていたため JST 朝の操作で前日日付が URL に入っていた問題を修正。`lib/date-param.ts` に統一実装（`formatReviewDateParam` / `parseReviewDateParam` / `buildReview*Path` / `writeReviewSearchParams`）
- **Daily Review 機能拡張 (1 commit)**: 確認待ちの予定リスト（`isUnconfirmedPlanned` を domain に分離）と計画外時間 KPI を追加。`FulfillmentScoreButtons` を抽出してタイムラインと確認待ちリストで共用。domain テスト 6 件追加

#### なぜ今日この作業をしたか

UTC 日付バグは URL 復元機能の導入（先週）によって初めて実害化した。以前は `?d=` を「遷移パラメータ」としてのみ使っていたが、URL 復元では「ページリロード後に読み取る永続的な state」として機能するようになった。JST +9h 環境では朝 9 時以前に `toISOString()` を呼ぶと前日の日付が得られるため、当日の朝に Review ページをリロードすると前日の Review が表示される状態だった。修正は書き込みと解釈を一点集中させることで、5 箇所に散在していた重複と UTC/ローカル混在を一掃した。

Daily Review の「確認待ちリスト」は、「計画どおりに記録されたかどうか確認する」という Daily Close の本質的なユーザー体験を前進させる機能。actual が plan と完全一致かつ未採点の終了済みエントリを「ユーザーが意識して確認していないもの」とみなし、採点タップで一件ずつ解消する体験を作った。`isUnconfirmedPlanned` の判定を domain 層に分離したのは、自動記録モデル（skipped_at ベース）への移行時に UI 側を変えずに中身だけ差し替えられるようにするため。設計の将来対応が今日のコードに織り込まれた形。

#### Claudeの所感

**URL を永続 state として扱った瞬間に時刻バグが顕在化する**: `toISOString()` は UTC 基準のため JST 環境では常にずれうる。「遷移のためだけのパラメータ」なら乗り換え先で再度ローカル日付を作ればいいが、「URL を直接読んで日付を決定する」設計だと UTC/ローカルのずれが直接表示に影響する。正午基準解釈（`T12:00:00` を補完）は最大 ±14h のタイムゾーンでも当日内に収まる、よく知られたロバストパターン。URL に日付文字列を書く設計は最初からローカル日付フォーマットを単一実装化しておくべきだった。

**domain 層の判定ロジック分離は「将来の差し替え口」を作る行為**: `isUnconfirmedPlanned` は「actual と plan が一致し、採点されておらず、終了済み」という複合条件。これを UI 側に直接書けばすぐ動くが、自動記録モデル移行後は `skipped_at` の有無や `isAutoRecorded` フラグが条件に加わる。分離していれば UI は変えず domain 関数の中身だけを差し替えればいい。設計判断を「今すぐ動く最小実装」と「将来の変更コスト最小化」のバランスで選んだ事例。

**`FulfillmentScoreButtons` の抽出タイミング**: 同じボタン群がタイムラインの Popover と確認待ちリストの 2 箇所に登場したタイミングで抽出した（3 回ルールより早い）。UI の重複はビジュアル一貫性の問題を必ず起こすため、「まったく同じ見た目が 2 箇所」は早期抽出の明確なシグナル。コンポーネントの粒度判断として、「同一 domain の同一 UI」は即抽出、「似ているが context が違う」は保留、という基準が機能した。

---

## 2026-06-10

### Review の粒度別ビュー完全再設計の日

11 コミット、41 ファイル変更（3682 行追加 / 481 行削除）。review 機能のみに集中した一日。`feat/review-granularity-redesign` ブランチで、`ReviewView` の全面再設計から各粒度ビュー（日/週/月/年）の新規実装、信頼性ガード、URL 復元、Storybook story、E2E smoke まで一気に実装した。

#### やったこと

- **ReviewView の dispatcher 化 (1 refactor commit)**: 442 行あった `ReviewView.tsx` を 51 行の粒度 dispatcher に縮小。データ取得ロジックを各粒度コンポーネントへ移譲し、ReviewView は「現在の粒度に応じて DailyReview / WeeklyReview / MonthlyReview / YearlyReview を切り替える」純粋な router 構造に
- **週次ビュー刷新 (1 feat commit)**: 所見（InsightSlot）+ KPI 行 + Time P/L + TagBalance + RhythmCharts + NextActionLink の構成で `WeeklyReview.tsx`（336 行）を新設。Time P/L（計画 vs 実績の損益）を最上位セクションに昇格させ、「記録合計 + トップタグ」は KPI 行の 1 指標に降格
- **日次ビュー新設 (2 feat commits)**: `DayTimelineComparison.tsx`（290 行）を中心に構成した `DailyReview.tsx`（270 行）を新設。計画 vs 実績のタイムラインを並べて表示し、充実度採点 UI を追加してインラインで評価を記録できるようにした
- **月次・年次ビュー新設 (1 feat commit)**: `MonthlyReview.tsx`（211 行）と `YearlyReview.tsx`（179 行）を新設。月次はパターン分析（EstimationAccuracy / TagBalance / MonthlyDailyChart）、年次は俯瞰（YearHeatmap / MonthlyTrendChart）の構成
- **週のリズム拡充 (1 feat commit)**: `RhythmCharts.tsx` の曜日別分布を週開始曜日（settings 依存）対応にし、前期間データをゴースト表示（グレー棒）で重ねられるように拡張
- **所見の信頼性ガード (1 feat commit)**: `InsightSlot` に `confidence` prop を追加。記録数が少ない期間は所見を表示せず、「沈黙も人格の一部」という copywriting.md の方針をコードに反映
- **URL 復元と prefetch 対応 (1 feat commit)**: `?g=&d=` URL パラメータを SSR hydration 時に prefetch と同期させ、deep link で正しい粒度・日付のページが表示される
- **Storybook story 追加 (1 test commit)**: DayTimelineComparison / MonthCharts / RhythmCharts / TagBalancePanel / InsightSlot / SummaryCard / NextActionLink の 7 コンポーネント分の story を追加
- **E2E スモーク (1 test commit)**: `/review` の 4 粒度 deep link が正しく描画されることを Playwright で検証（`review-granularity.spec.ts`、54 行）
- **設計書追加 (1 docs commit)**: `apps/storybook/docs/product/projects/review-granularity-redesign/overview.md`（271 行）にユーザー合意済みの設計方針を記録

#### なぜ今日この作業をしたか

Review ページは 5 月に Stats 4 タブ → 単一ページへ集約したが、「全粒度で同じレイアウトを見せる」という構造上の課題が残っていた。日次で「空き時間の大きな数字」を見せても意味は薄く、年次でタグ別詳細リストを見せるのは重い。粒度ごとに「答えるべき問い」が違うのに、同じ composition を使い回すのは UX として間違っていた。

設計書を先に確定し実装はその後というワークフローを採用した。`overview.md`（271 行）を最初のコミットとして積むことで、「なぜこの構成なのか」の根拠を git 履歴に残した。設計フェーズでの議論（タブ再導入 vs 粒度別 composition、AI 生成所見 vs rule-based 所見）の決着も設計書に収録されている。

Time P/L を主役に据えた判断は、Dayopt の差別化軸の明確化でもある。Toggl は actual しか持たず、Google カレンダーは planned しか持たない。両方を持つ Dayopt だけが「計画と現実のずれ」を見せられる。この差別化データを Review の最上位セクションに置くことで、ユーザーが Review ページを開いた瞬間に Dayopt の独自価値に触れる設計にした。

#### Claudeの所感

**442 行→51 行の refactor が設計の正しさを測る尺度に**: `ReviewView.tsx` が 442 行になっていた根本原因は、「全粒度共通の composition」という暗黙の仮定が守れていなかったから。粒度ごとに if/switch で分岐するコードが肥大化した結果だった。dispatcher に縮小できたのは、「粒度 = 別の問い」という設計原則を徹底したから。コンポーネントのサイズは、設計の明確さのプロキシとして機能する。

**信頼性ガードは「沈黙を選ぶ UI」の実装**: copywriting.md には「データが不足しているとき、研究者は何も言わない」と書いてある。`InsightSlot` の `confidence` prop はこれをコードで表現したもの。「条件を満たさない場合は null を返す」ではなく、「confidence が LOW なら表示しない」という設計は、UI コンポーネントが copywriting の人格をコントロールする責任を明示的に持つことを示す。デザインシステムと voice/tone の統合の一例。

**E2E smoke を最後に置く意味**: 粒度切替のような URL state × SSR × hydration が絡む機能は、unit test だけでは「動いているか」を確認しきれない。`review-granularity.spec.ts` を最後のコミットとして積むことで、実装が完了した後にシステム全体として動作することを明示的に証明した。smoke test は「実装した」から「動いている」への橋渡し。

---

## 2026-06-08

### 予定だけエントリの表示完全修正と認証セキュリティ強化の日

14 コミット（内 6 件は dependabot 自動更新）。手動実装は entry / calendar / auth の 3 領域。午前は認証セキュリティ修正と EntryCard の UI 改善、午後はカレンダーの「予定だけエントリ」表示バグを根本から修正した。

#### やったこと

- **予定だけエントリの表示修正 (3 commits)**: `entry-adapter.ts` で `actual_start_time` が null の場合 `return null` していた旧実装を削除し、`planned` origin なら `start_time` / `end_time` を表示位置として使うよう変更。`CalendarGridContent` の `overflow-hidden` を `overflow-visible` に変更して予定レイヤーの高さ揃えを修正。`EntryCard` の planned レイヤーがグリッドサイズに揃うようコンポーネント側も調整。テスト追加（`entry-adapter.test.ts` / `EntryRenderer.test.tsx` / `EntryCard.test.tsx`）
- **EntryCard UI 改善 (2 commits)**: 予定ブロックの左上角・左下角を直角化（`rounded-tl-none rounded-bl-none`）。時間表示を左端フラッシュに変更して Day / Week / 複数 View での統一感を確保。Storybook story に時間状態バリアント（predicted / actual / plan-only）を追加
- **認証セキュリティ修正 (2 commits)**: パスワード変更前に `signInWithPassword` で現在パスワードを検証するフローを追加。Supabase の `updateUser` は再認証を要求しないため、ユーザーが意図した変更かどうかを手前で確認する。テスト 142 行追加
- **作成プレビュースタイル統一 (1 commit)**: `DraftEntryBlock` の左アクセントボーダー（`border-l-2`）を削除し upcoming エントリと視覚的に統一
- **依存パッケージ更新 (6 dependabot PRs)**: production 28 件、dev/testing/storybook/linting/types/other の各グループをまとめて更新

#### なぜ今日この作業をしたか

「予定だけエントリ」の修正は、Dayopt の中核的な差別化要素である「計画 × 記録の一体化」に直結するバグだった。ユーザーが事前に予定を入れても（origin: 'planned'）、実際の記録（actual_start_time）がない限りカレンダーに一切表示されない状態が続いていた。これは「タイムボクシングした予定が見えない」という重大な UX の穴であり、計画フェーズでのカレンダー活用を妨げていた。修正は entry-adapter 1 ファイルの null ガード削除が起点だったが、表示崩れ防止のために CalendarGridContent と EntryCard 側の調整も必要で、3 コミットに分かれた。

認証のパスワード再確認は、セキュリティ上の漏れを塞ぐ修正だった。Supabase の `updateUser` は有効なセッショントークンさえあれば現パスワード検証なしで変更できる。この設計は PKCE フローにおける session 継続を前提としており、ウェブアプリの UX では一見問題ないが、「画面ロック中に第三者が操作できる」などのリスクを残す。今回 `signInWithPassword` による現パスワード検証を挟むことで、Supabase が持つ session 継続の仕様に依存せずに安全な変更フローを実現した。

#### Claudeの所感

**「null ガード = 安全」という思い込みが機能バグになる例**: `entry-adapter.ts` の `return null` は「不完全なデータを表示しない」という防御的コードに見える。しかし planned エントリにとって `actual_start_time` が null であることは「エラー状態」ではなく「まだ記録していない正常な状態」だった。型レベルで `planned` origin の場合は `actual_*` が null でも valid であることを表現できていれば、このバグは早期に気づけた可能性がある。DB スキーマと型定義の semantic gap がバグの温床になった事例。

**`overflow-hidden` はレイアウト系バグの隠れた原因になりやすい**: 絶対配置された子要素がグリッドからはみ出ることを期待するコンポーネント（リサイズハンドル、ドラッグプレビュー）には `overflow-hidden` が致命的な影響を与える。今回の修正で `overflow-visible` に変更したが、これはクロスデイドラッグ時に別のはみ出し制御が必要だった旧実装の残骸だった。機能削除時に関連する CSS も一緒に削除するルールの重要性を示している。

**テストのモックビルダーで `null` と `undefined` を区別するパターン**: `makeEntry` 関数で `overrides.actual_start_time ?? entry.start_time` の書き方は、呼び出し元が `actual_start_time: null` を渡しても null を無視して `entry.start_time` にフォールバックしてしまう。`Object.prototype.hasOwnProperty.call(overrides, key)` で「キーが存在するかどうか」を判定することで、null の明示的な指定と未指定を区別できる。テストの表現力に直結するテクニック。

---

## 2026-06-05

### カレンダーUI大ポリッシュデー——ConflictOverlay統一とEntryCard全面磨き直し

24 コミット。entry・calendar・tags・supabase・env の 5 領域を横断した。午前はカレンダーの重複表示を `ConflictOverlay` に統一し、午後は `EntryCard` のビジュアル細部（タグアイコン・色・位置揃え・破線枠）を徹底的に修正した。`ColonTagLabel` の廃止も合わせて実施。

#### やったこと

- **ConflictOverlay 統一 (6 commits)**: リサイズ・新規ドラッグ選択・タグ下書きと、重複表示が3箇所に散在していたのを `ConflictOverlay` コンポーネントに一本化。ゴーストと同一の視覚言語で統一し、左角をカードに揃えるピクセル修正も含む。`ConflictOverlay.stories.tsx` を新規追加
- **EntryCard ビジュアルポリッシュ (7 commits)**: タグアイコンをカードに表示、タイトルアイコン追加に伴う時間テキスト位置の揃え直し、予定外カードの破線枠が左アクセント帯を横切る問題の修正、延長記録の opacity 統一、upcoming の地の色を actual と統一、複数 View での時間テキスト折り返し防止
- **ColonTagLabel 廃止 (2 commits)**: コロン区切りのタグ階層表示コンポーネントを削除。陳腐化した Story も同時に削除
- **Supabase RPC 権限強化 (3 commits)**: RPC helper functions への `EXECUTE` 権限を `authenticated` ロールに付与、privilege hardening を migration として記録
- **環境変数 optional 化 (2 commits)**: Stripe webhook secret をローカル開発では必須でなくなるよう `env` バリデーションを修正

#### なぜ今日この作業をしたか

カレンダー UI の重複表示は、リサイズ・ドラッグ・下書きそれぞれが独自実装を持っていた。見た目は似ているのに内部が 3 分岐になっており、1 か所修正しても他の 2 か所には反映されない構造だった。`ConflictOverlay` への統一は「同じ概念を同じ実装にする」という技術的一貫性の整理で、今後の変更コストを大幅に削減できる。

`EntryCard` のポリッシュは、前日にタグアイコンを追加した副作用として生じたレイアウトのズレ修正が起点だった。アイコンが加わると時間テキストの開始位置が変わり、それを直すと破線枠・背景色・折り返しなど隣接する視覚要素のギャップが浮き彫りになる——という連鎖的な修正だった。「直したら別の問題が見える」を繰り返すことでカード全体のクオリティが底上げされた。

`ColonTagLabel` の廃止は、コロン階層表示がユーザーに伝わりにくいという設計判断からの帰結。コンポーネントとして独立していたが、実際に使われているのは限定的なユースケースで、`TagBadgeList` などで代替できていた。

#### Claudeの所感

**「統一」はリファクタリングではなく設計の修正**: `ConflictOverlay` の統一は表面的にはコードの整理に見えるが、本質は「重複という概念が実装に 3 つの身体を持っていた」問題の解消だ。同じ視覚概念が複数の実装に分裂すると、修正の際に「どれを直せばいいのか」の認知コストが生まれ続ける。単一出口を持つコンポーネントは、将来の修正コストを下げる設計上の投資だった。

**UI ポリッシュのカスケード現象**: タグアイコン追加という 1 つの変更が、時間位置・破線枠・背景色・折り返しという 4 方向の修正を引き起こした。これは UI の各要素が暗黙の前提（「アイコンはない」「テキストは特定の幅を取る」）の上に成立していたことを示す。ポリッシュ作業は往々にしてこの連鎖を辿る形になり、「1 つ直したら次が見える」という非線形な進み方をする。コミット粒度を細かく保ったことで、各修正の意図が後から追跡しやすくなっている。

**RPC 権限は migration として記録する価値がある**: Supabase の RPC helper function への権限付与は、一見メンテナンス作業に見えるが、「誰がどの関数を呼べるか」というセキュリティ境界の変更だ。migration ファイルとして残すことで、将来 RLS や権限周りのデバッグ時に「いつ、何の意図で権限を付与したか」が Git 履歴から追える。

---

## 2026-06-04

### モノレポ移行後の .claude/ 一括監査と Supabase Preview Branching 導入の日

8 コミット。変更ファイルは 50 本超。午前は Supabase Preview Branching への移行（PR #1256、#1257）、午後は `.claude/` 全体の monorepo 対応監査（PR #1258）を実施。インフラ層と AI 支援層の両方を一日で整備した。

#### やったこと

- **Supabase Preview Branching 移行 (2 commits)**: `supabase/config.toml` を preview branching 対応に更新。`supabase-migration.yml` GitHub Actions ワークフローを廃止し Supabase CLI の preview branch 機能に一本化。Auth SMTP を preview 環境で無効化し、seed をpreview-safe defaults に更新。production migration history の欠損も同日復元
- **.claude/ monorepo 対応 (6 commits → PR #1258)**: `.mcp.json` に playwright・github MCP を追加。`skill/` と `rules/` 全体のパス（`src/` → `apps/product/src/`）、コマンド（`npm run` → `pnpm`）、別リポジトリ参照を一括修正。skill 許可リストの stale エントリ（`less-permission-prompts`）を実名（`fewer-permission-prompts`）に整合。コードレビュー指摘の grep パスと vitest 引数も修正

#### なぜ今日この作業をしたか

Supabase Preview Branching は、PR ごとにマイグレーションを独立した環境で検証できる仕組みで、「本番 DB を直接触らずに migration を確認する」という長期的な安全弁として重要だった。旧来の `supabase-migration.yml` は CI 上で production に直接 push する構造であり、リスクの高い運用だった。移行後は preview branch がマイグレーション確認の主経路となり、production push は明示的な merge 操作のみに絞られる。

`.claude/` 監査は monorepo 移行（単一 Next.js app → apps/product + apps/web + packages/\*）が完了した後に積み残されていたメンテナンス負債の解消だった。旧構造のパス参照が残ったままでは Claude の skill/rules がコードベースと乖離し続ける。今日一括で直したことで、以降の AI セッションで正確なパス案内ができるようになった。playwright・github MCP の追加は、「UI 変更の視覚確認を Claude Code に任せる」「PR 参照を URL コピペなしで取得する」という運用改善の第一歩。

#### Claudeの所感

**AI 支援系の設定ファイルにも技術的負債が積む**: `.claude/` 配下の rules・skills は「AI への指示書」だが、コードと同じく旧構造への参照が残り続ける。今回の監査で 26 ファイル・154 行の変更が必要だったことは、monorepo 移行後の整理が後回しになっていた規模の大きさを示している。AI 支援ファイルは通常のコードと同じ保守コストを持つという認識が必要。

**Preview Branching は「migration を PR レビューで止められる」唯一の構造**: 旧来の GitHub Actions 直 push は「merge されたら即 production 反映」だった。Preview Branching では Supabase の branch merge が別操作になるため、「コードのレビュー」と「DB 変更のレビュー」を分離できる。これは solo dev でも有効で、後日見返す際にも migration の意図が PR に残る。

**Auth SMTP の preview 無効化は見落としやすいトラップ**: Preview Branch はローカル supabase とは別の Supabase project instance を持つが、Auth SMTP 設定はそのまま引き継がれる。preview 環境で実際にメールを送信しようとすると SMTP エラーになる。今回 `config.toml` で明示的に無効化したが、この挙動は Supabase のドキュメントには目立たない形でしか書かれておらず、実際に遭遇して初めて分かる類のものだった。

---
