date: 2026-05-25
commits: 10
areas: [packages, domain, entry, tags, review, deps]

decisions:

- Dayopt domain model package を新設 (#1207)。`@dayopt/domain` として workspace に追加し、apps と他 packages から import 可能な domain 型の置き場を確立
- `lib/types/{entry,calendar-event}` を `features/entry/types/` へ移動 (#1210)。entry 関連型を feature ownership へ
- review の `timePL.derivers` から pure derivation と types を domain へ分離 (#1209)
- `tag-colors` と `color-palette-picker` を `features/tags` へ移動 (#1208)。tag 関連の color logic を feature 配下へ
- 横断 ownership exception を整理し、lib → feature の所属修正を実施 (#1206)
- `@types/react` を 19.2.14 に pin (#1217)。dependabot blocker 対応として固定

breaking:

- `lib/types/entry`, `lib/types/calendar-event` の path 廃止。`features/entry/types/` から import

files_of_note:

- packages/domain/ # 新規 Dayopt domain model package
- apps/product/src/features/entry/types/ # lib/types から移動
- apps/product/src/features/tags/ # tag-colors / color-palette-picker の新しい所属

next:

- [ ] entry statistics.ts の本格的な domain 切り出し（pure aggregation / transformer の段階的分離）
- [ ] tag-service.ts の merge / ungroupTags から pure logic 切り出し
- [ ] billing model の packages 化検討
