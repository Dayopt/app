date: 2026-04-20
commits: 10
areas:
[
sidebar,
entry,
shell,
supabase,
secrets,
infra,
deps,
]

decisions:

- features/history を丸ごと撤去（RecentBlocks / tRPC history router / Feature Boundary Layer 2 登録）
- サイドバー履歴に依存していた MobileCreateSheet / MobileHistoryStrip / MobileCreateButton も同時撤去
- Supabase を単一 project 運用に統一（ローンチ前の簡易構成、dev / preview / production すべて yvglwblxrnrenfifsnje を参照）
- 1Password を 3 vault 構成（Dayopt-Staging / Dayopt-Production / Dayopt-Shared）で運用開始
- op-run 移行 Phase 1 として npm run dev:op / env:check を導入
- 旧 Production project (qloztwfbrbqtjijxicnd) は空だったため破棄対象にせず repo 側の参照だけ差し替え
- toast キー sidebar.history.\* を entry.toast.\* に移動（実際の利用箇所 useBlockPlace と整合させる）

conventions:

- toast key の namespace は機能名ではなく実際の利用箇所（hook / component）と整合させる
- ローンチ前の Supabase 運用は単一 project、ローンチ後に 1 project + persistent staging branch + ephemeral preview branches へ移行する
- 1Password は op:// reference pattern で secrets を扱い、.op-env.local と .env.local は両方 gitignore する
- setup-1password.sh は初回専用 script として dry-run default、--execute で実行する形を維持する

breaking:

- features/history を削除（tRPC の history router、entry mutation からの invalidate、Eagle 自動タグ付与、Sidebar.docs.mdx の dotVariant 節まで含む）
- MobileCreateSheet / MobileHistoryStrip / MobileCreateButton / useShellStore の mobileCreate sheet type を撤去
- mobile-layout の余白を BottomTabBar 分 (pb-16) のみに戻した
- package.json から types:generate:staging を削除、types:generate は yvglwblxrnrenfifsnje 向けに統一
- .github/workflows/supabase-migration.yml から migrate-staging job を削除、migrate-production を yvglwb... に統一
- toast i18n キー sidebar.history.{placed, overlapError} を entry.toast.{placed, overlapError} にリネーム

learned:

- 旧 Production project が実質空で Staging project 側が本来の開発・検証環境として使われていた。forward-compatible な docs 更新を先に commit した後で現状に即した refactor を重ねる順序で、作業履歴が壊れず方針転換できた
- Supabase branching は Pro plan 契約が前提で、無料枠では persistent staging branch / ephemeral preview branches を作成できない。ローンチ前は単一 project 運用が現実的
- supabase db push は --project-ref 非対応で、link 済み project に対して実行される
- 1Password op-run は .op-env.local の op:// reference を解決して環境変数注入する。生キーを Claude のコンテキストに乗せずに dev サーバを起動できる
- features barrel 削除時は tRPC appRouter の import、eslint feature boundary 登録、Eagle scripts の自動タグ付与、messages/\*/sidebar.json、Sidebar.docs.mdx の mention まで全部追う必要がある

tried_and_failed:

- 最初は 1 project + persistent staging branch + ephemeral preview branches の運用方針で docs（SKILL.md / architecture.md / migration-checklist.md 等）を全面書き換えた。しかし Pro 契約前に branches が作れないことと、旧 Production project が空で Staging 側が実運用だったことに気付き、同日中に「ローンチ前は単一 project 運用」へと方針を戻した。docs は forward-compatible な target state として残し、実運用ドキュメントだけ単一 project に書き換える 2 段階の commit 構造で収束した

files_of_note:

- .claude/skills/supabase/SKILL.md # 単一 project 運用に全面書き換え、branching 構成は「ローンチ後の target state」として位置付け
- .storybook/docs/operations/secrets.mdx # 1Password 3 vault 構成 / op:// reference pattern / NOTES テンプレートを常駐化
- scripts/setup-1password.sh # 初回専用 script、dry-run default、--execute で実行
- docs/development/migration-checklist.md # 暫定フロー節で Pro 契約前の単一 project 運用を明示
- next.config.mjs # images.remotePatterns の hostname を yvglwb... に変更
- .github/workflows/supabase-migration.yml # transitional ヘッダコメント、migrate-staging 削除
- .gitignore # .op-env.local を exclude

next:

- [ ] Pro plan 契約後に 1 project + persistent staging branch + ephemeral preview branches へ再移行（workflow 置換、package.json scripts 再設計、GitHub integration 有効化、staging branch 作成）
- [ ] 1Password op-run Phase 2 以降（Phase 1 は dev:op / env:check まで）
- [ ] user-global skill の Opus 4.7 チューニング（~/.claude/skills/ 配下、04-17 繰越）
- [ ] effort level の skill 単位動的切替運用（14 skill の 3 カテゴリ分類が先行要件、04-17 繰越）
- [ ] adversarial review subagent の設計（.claude/agents/ 新設、04-17 繰越）
