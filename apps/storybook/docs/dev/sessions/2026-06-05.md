date: 2026-06-05
commits: 24
areas: [entry, calendar, tags, supabase, env]

decisions:

- `ConflictOverlay` を重複表示の単一出口として採用。ドラッグ選択・リサイズ・タグ下書きすべてのケースでこのコンポーネントに統一
- `ColonTagLabel`（コロン区切りタグ階層表示）を廃止。`TagBadgeList` 等で代替
- `EntryCard` にタグアイコン（tagIcon）を組み込み。アイコン付きタグの視覚表現を標準化
- `upcoming`（予定エントリ）の背景色を `actual`（記録済み）と統一し視覚的一貫性を確保
- Stripe webhook secret をローカル開発では optional 化

conventions:

- エントリカードの重複表示は `ConflictOverlay` に一本化する。ドラッグ・リサイズ・下書きすべて同じコンポーネントで表現

breaking:

- `apps/product/src/lib/components/ui/colon-tag-label.tsx` 削除（ColonTagLabel コンポーネント廃止）

files_of_note:

- apps/product/src/features/calendar/components/views/shared/components/ConflictOverlay.tsx # 重複表示統一コンポーネント
- apps/product/src/features/entry/components/card/EntryCard.tsx # タグアイコン + ポリッシュ修正（未コミット変更あり）
- apps/product/src/lib/components/ui/colon-tag-label.tsx # 削除済み

next:

- [ ] EntryCard 未コミット変更（planned layer 左角直角化、renderAsPlanOnly 対応）をコミット
- [ ] feat/calendar-ui-polish ブランチの残り作業を確認してPRを作成
