date: 2026-05-27
commits: 11
areas: [entry, monorepo, product/ui, docs]

decisions:

- package foundation を第一段階「運用可能状態」として宣言。次フェーズは「既存 apps が shared package を低リスクに使う段階」と PackagesOverview.mdx に明記（#1231）
- statistics.ts 残存 inline RPC unpacking を statistics-kpi-unpackers.ts / statistics-energy-map-transform.ts に切り出し完全クリーンアップ（#1232）
- product app で @dayopt/ui を初実利用。Button / Badge / EmptyState / ErrorState を @/lib/components/ui/\* から shared package 参照に移行

learned:

- '@dayopt/ui の Tailwind source scan は globals.css に @source "../../../../../packages/ui/src" を追加しないと product ビルドでスタイルが消える'
- t3-env の全スキーマ (env) を URL のみ用途の経路（metadata / sitemap）に通すと Supabase 系の必須 env 欠如でビルドが壊れる。URL のみ使う箇所は process.env.\* を直接参照する

conventions:

- '@dayopt/ui を product に追加する際は globals.css に @source と @import "@dayopt/design/theme.css" を合わせて追加する'

files_of_note:

- apps/product/src/features/entry/server/statistics-kpi-unpackers.ts # 新規: KPI 5 unpacker 関数
- apps/product/src/features/entry/server/statistics-energy-map-transform.ts # 新規: energy map transform
- apps/product/src/lib/styles/globals.css # @dayopt/ui source scan + @dayopt/design theme import 追加
- apps/product/src/lib/app-url.ts # env (t3-env) から process.env.\* 直接参照に変更

next:

- [ ] '@dayopt/ui の product 実利用を他コンポーネントにも展開（Button / Badge 以外の shadcn primitives）'
- [ ] packages/config の残り hardcode 置換
- [ ] auth boundaries 整理の他 router への展開
