date: 2026-05-20
commits: 12
areas: [monorepo, web, entry, calendar, stores, ci, deps]

decisions:

- 既存 web repo を `apps/web` として monorepo に取り込む (#1170)。product と並列の workspace 構造に統一
- `apps/product` workspace へリネーム（旧 `app`）。monorepo 内の workspace 名を「役割を表す名詞」で揃える
- entry 時間モデルの domain 層を `features/entry/domain` として新設 (#1173)。lib/ ではなく feature 配下に置く方針を確立
- `useCalendarSettingsStore` から user preference を別 store に分離 (#1180)。store の責務単位を「設定 vs ユーザー嗜好」で切る
- web build を CI quality gate に追加 (#1174)。`apps/web` の build 失敗もブロッカー扱い
- web docs feature を Vercel deploy 対象に戻す (#1175)。monorepo 移行時に外れていたものを復帰

breaking:

- `useCalendarSettingsStore` の `sleepSchedule` state を削除 (#1182)。dead state として除去

files_of_note:

- apps/product/ # 旧 app/ からリネーム
- apps/web/ # 既存 web repo を取り込み
- apps/product/src/features/entry/domain/ # 時間モデルの domain 層を新設

next:

- [ ] feature-colocation migration を他 feature にも展開（calendar, chronotype, tags 等）
- [ ] monorepo 全体で workspace command の集約（root から各 workspace を呼べる形）
