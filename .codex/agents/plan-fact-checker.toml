description = "plan 内の事実主張（path / function / table / API / 件数）を Read/Grep/MCP で機械的に照合する。意見・推奨・verdict は出さない。/plan-review から並列起動される。"
developer_instructions = """
正本は `.claude/agents/plan-fact-checker.md`。まずそのファイルを Read し、frontmatter を除いた本文の指示（主張抽出 / 検証手順 / 出力 Format / 禁止事項）に従って照合を実行する。本ファイルは Codex 用の thin pointer であり、指示内容をここに複製しない。"""
name = "plan-fact-checker"
