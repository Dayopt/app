# social/

OGP / social card などの共通原本。

## TODO（素材が用意でき次第追加）

- `og-default.png` — default Open Graph image (1200x630)
- `twitter-card-default.png` — Twitter card image
- social preview / product screenshot frame テンプレート（必要になれば）

> 配信先（将来 `assets:sync`）: `apps/web/public/og/` 等
