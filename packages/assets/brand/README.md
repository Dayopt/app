# brand/

Dayopt のブランド原本（SVG）。`<Logo />` の React 実装は `@dayopt/components` 側（SVG inline）。

## 現状

- `logo-mark.svg` — symbol mark（`@dayopt/components` の `Logo` と同一 path）

## TODO（素材が用意でき次第追加）

- `logo.svg` — mark + wordmark の lockup 原本
- `logo-wordmark.svg` — wordmark（文字列原本。現状 `<Logo>` は font で描画）
- `logo-monochrome.svg` — モノクロ版
- app icon source（`app-icons/` 用のマスター）
