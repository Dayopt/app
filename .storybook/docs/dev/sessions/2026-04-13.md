---
date: 2026-04-13
commits: 42
areas:
  [
    feature-structure,
    palette,
    shell,
    calendar,
    entry,
    auth,
    tags,
    i18n,
    storybook,
    error-handling,
    db,
    ci,
  ]
---

decisions:

- src/直下の散在ディレクトリ(platform, hooks, types, stores, shell, stories, styles, test, components)をlib/またはfeatures/配下に全統合
- パレット機能を完全削除（feature, tRPC, server, UI連携, i18n, tests, DBテーブル）
- エラーハンドリングをServiceError + handleServiceErrorパターンに統一
- feature boundary DAGルールを再有効化
- モバイルフッター上部に履歴ストリップを追加（パレット廃止後のクイックアクセス代替）
- DayViewモバイルヘッダーを2行→1行に統合

conventions:

- src/直下に散在ディレクトリを置かない — lib/ か features/ のみ
- ServiceError + handleServiceError がエラーハンドリングの標準パターン
- feature内ファイル構造はfeature-boundaries.mdに文書化したパターンに従う
- 全featureはbarrel(index.ts)経由でのみexport

breaking:

- パレット機能全体（features/palette/, tRPC router, palette_items table DROP）
- src/shell/, src/stores/, src/platform/, src/hooks/, src/types/, src/styles/, src/stories/ ディレクトリ廃止
- test/, components/ を lib/配下に統合し旧パス廃止
- .github/CODEOWNERS 削除

learned:

- turbopackはserver-only境界違反をビルドエラーとして検出する（webpackより厳格）
- Next.js middlewareからenv.tsをインポートするとエラー — process.env直接参照が必要
- storybook-setup用ファイルはtsconfig.jsonのexcludeに入れないとtscビルドに巻き込まれる

tried_and_failed:

- recovery-codesコンポーネントにserver-onlyを直接importする方式 → server action経由に分離して解決

files_of_note:

- .claude/rules/feature-boundaries.md # feature構造ルールを新規文書化
- supabase/migrations/\*\_drop_palette_items.sql # staging実行待ち

next:

- [ ] palette_items DROPマイグレーションをstagingで実行・確認
- [ ] 履歴ストリップのモバイルUX検証
- [ ] unpushed 11コミットのpush
