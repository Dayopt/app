date: 2026-04-16
commits: 27
areas: [search, toast, dialog, inline-banner, shell, stats, settings, auth, inspector, sw, storybook, db]

decisions:

- search featureを完全削除（cmdk依存含む）
- toastをミニマル1行デザインに刷新（info/warningを廃止、success/error/loading/promiseの4種に絞る）
- confirm-dialogをAlertDialogベースに書き換え
- dialogにresponsive自動切り替え（PC=Dialog, モバイル=Drawer）とsize propを追加
- OfflineIndicatorをinline-bannerコンポーネントに置換
- モバイルボトムバーにスクロール連動auto-hideを実装
- 4月前半の全変更をバッチPR #1047としてmainにマージ

conventions:

- toastはミニマル1行（title + optional description）。success/error/loading/promiseの4種のみ
- confirm-dialogはAlertDialogベースで実装する（オーバーレイクリックで閉じない）
- dialogはPC=Dialog, モバイル=Drawerの自動切り替えをデフォルトにする

breaking:

- search feature 完全削除（features/search/, lib/hooks/use-global-search.ts, ui/command.tsx）
- cmdk パッケージ削除
- toast.info(), toast.warning() 廃止

learned:

- AlertDialogはオーバーレイクリックで閉じないため、破壊的操作の確認に適している。DialogとAlertDialogの使い分けが明確になった
- useHideOnScrollはIntersectionObserverよりscrollイベント+requestAnimationFrameの方がシンプルで信頼性が高い
- inline-bannerパターンはOfflineIndicatorのようなページ横断の状態表示に適している。トーストと違い常時表示で視認性が高い

files_of_note:

- src/lib/components/ui/inline-banner.tsx # 新規: ページ横断の状態バナー
- src/lib/hooks/useHideOnScroll.ts # 新規: スクロール方向検出フック
- src/lib/hooks/useInlineBanner.ts # 新規: inline-banner状態管理フック
- supabase/migrations/20260416000000_fix_time_pl_available_minutes.sql # time P/L修正
- supabase/migrations/20260416100000_add_update_personalization_rpc.sql # personalization競合修正
- supabase/migrations/20260417100000_backfill_auth_metadata_full_name.sql # auth表示名バックフィル

next:

- [ ] Staging に db push（3件のマイグレーション適用確認）
- [ ] types:generate:staging で database.types.ts を正式再生成
- [ ] パフォーマンスの実測（Lighthouse / Web Vitals）
- [ ] entry_instances テーブルの要否判断（繰り返し機能の方針次第）
- [ ] toast刷新後の全画面動作確認
