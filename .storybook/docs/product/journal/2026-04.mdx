import { Meta } from '@storybook/blocks';

<Meta title="Strategy/Journal/2026-04" />

# 2026年4月 Diary

---

## 2026-04-20

### 履歴機能の撤去と Supabase 運用モデルの現実路線化

10コミット、app 側の実機能変更3件 + インフラ / 運用整備5件 + Dependabot 2件。午前〜夜にかけて「履歴機能を丸ごと撤去」「Supabase 単一 project 運用への収束」「1Password 3 vault 運用の立ち上げ」という3つの独立したテーマが並走した1日。午前中に UI 側の撤去作業を一気に終わらせ、午後から夜にかけてインフラ・シークレット管理の再設計に取り組んだ。

#### やったこと

**Sidebar 履歴機能の完全撤去（3件）**: toast キーのリネーム (sidebar.history.\* → entry.toast.\*) を入口に、MobileCreateSheet / MobileHistoryStrip / MobileCreateButton の削除、features/history の barrel 撤去、tRPC history router の除去、Feature Boundary Layer 2 登録の削除、messages/\*/sidebar.json の該当キー除去、Sidebar.docs.mdx の dotVariant 節整理、Eagle 自動タグ付与の参照削除まで一気通貫で実施。

**Supabase 運用モデルの 2 段階方針転換（4件）**: 午前中にまず 1 project + persistent staging branch + ephemeral preview branches の運用方針で SKILL.md / architecture.md / migration-checklist.md / Environments.mdx / CLAUDE.md を全面書き換え。その後 CLAUDE.md に 1Password op-run Phase 1 コマンドを追記、repo 側の forward-compatible な config 更新（supabase/config.toml / migration-checklist.md / workflow 注記）を完了。午前の途中で「旧 Production project が実質空で Staging project 側が実運用だった」ことに気付き、午前の終わりに単一 project 運用へと方針を戻す refactor commit を入れた。types:generate:staging の削除、migration workflow の migrate-staging job 撤去、next.config.mjs の images hostname 変更まで含む。

**1Password 3 vault 運用の立ち上げ（1件）**: .storybook/docs/operations/secrets.mdx に Dayopt-Staging / Dayopt-Production / Dayopt-Shared の 3 vault 構成、op:// reference pattern、NOTES テンプレートを常駐化。scripts/setup-1password.sh に 3 vault + 22 item 骨組みを CLI で作成する初回専用 script（dry-run default、--execute で実行）を追加。.gitignore に .op-env.local を exclude。

**Dependabot（2件）**: production group 14 updates、development group 39 updates。web 側でも major group 7 updates が自動マージ。

#### なぜ今日この作業をしたか

サイドバー履歴は 4 月前半のバッチマージ後も「実質 RecentBlocks だけを表示する装飾」として残っていた。モバイルの MobileCreateSheet / MobileHistoryStrip は中身が RecentBlocks しかなく、独立した存在意義がない。4-17 までのモバイル UX 仕上げで tap=Inspector / longpress=ドラッグ の操作体系が固まった今、履歴という「別の入口」は逆に操作体系の純度を下げる要因になっていた。撤去は toast キーのリネーム → 使っていた UI コンポーネントの削除 → 機能本体の削除、の3 commit に分けることで、各段階での型エラー・参照漏れを段階的に潰せる構造にした。

Supabase 運用の方針転換は朝の時点で「1 project + branches」モデルでドキュメント整備を進めていた途中で、Pro plan 未契約の現状では branches が作れないこと、そして旧 Production project が空で Staging 側が本来の開発環境だったことが発覚した。選択肢は「Pro を今すぐ契約して target state に合わせる」「単一 project 運用を暫定として明文化する」の2つで、ローンチ前の simplicity を優先して後者を選んだ。forward-compatible な docs 更新を先に commit した上で、現状に即した refactor を重ねる順序にしたため、target state の記述は target state として残り、実運用ドキュメントだけが単一 project 運用に書き換わる綺麗な2段階構造になった。

1Password 3 vault 運用は、op-run 経由の secrets 注入で「生キーを Claude のコンテキストに乗せない」運用を成立させるための前準備。dev:op / env:check コマンドを Phase 1 として CLAUDE.md に追記し、setup-1password.sh は初回専用 script として冪等性より「一発で骨組みを作れる」ことを優先した。

#### Claudeの所感

**「撤去系タスクは toast key → UI component → 機能本体の順で分ける」は型チェックと review がしやすい構造**: 今回の履歴撤去は3 commit で進んだが、先に toast key を移動させておくと「この key の移動先が正しいか」の review が独立した小さい単位で完結する。続く UI component 撤去では useShellStore の sheet type を外し、最後の features/history 削除で tRPC / Feature Boundary / Eagle scripts まで追う。commit 境界を「何を壊しうるか」で切ると、各段階で型エラーの情報密度が高く保たれる。同じパターンは今後の feature 撤去でも再利用できそう。

**2 段階 commit で方針転換を構造化する手法が機能した**: Supabase 運用モデルの変更は、理想状態の docs 書き換え → 現状の refactor という順序にしたことで、target state の記述を捨てずに現状運用に収束できた。逆順（現状 refactor から始めて target state を後で書く）だと「なぜこう書き換えるのか」の根拠が commit に残らず、後日の再移行時に意図を辿りにくくなる。Pro plan 契約後に再度 branching 構成へ移行する際、今日の docs commit が出発点として機能するはず。

**secrets 運用の前準備は ROI が見えにくいが、Claude との協業文脈では投資価値が高い**: 1Password 3 vault 構成 + op:// reference pattern を常駐化しただけでは実運用の価値は可視化されない。しかし Phase 2 以降で CI / preview env にも op-run を拡張するとき、今日の vault 構造と reference pattern が出発点になる。また、Claude に .env.local を直接触らせない運用（.proposed file 経由で人間が merge）が前提にある以上、生キーを含む file が増えるほど Claude のコンテキスト汚染リスクも上がる。vault 構造で secrets を「名前」として扱える状態にすることは、AI 協業前提のプロジェクトでは基礎体力そのものになる。

## 2026-04-17

### モバイル UX 仕上げと Opus 4.7 移行の周辺整備

31コミット、70ファイル変更（+2,567 / -981行）。billing ダイアログ・モバイルカレンダー・タグ UI の実機能変更と、Opus 4.7 migration 後の周辺ドキュメント・ルール整備が並走した1日。午前中にプロダクト機能を一気に仕上げ、午後は AI 協業まわりの規範体系を組み直した。

#### やったこと

**Billing ダイアログ一式（5件）**: トライアル終了と決済エラーの2系統で、ダイアログ本体 → フック (`useTrialEndedDialog` / `usePaymentErrorDialog`) → base-layout-content への統合の順に積み上げ。決済エラーはさらに `useAppInlineBanner` 経由でバナー表示系も整備し、stage 1 として実装を固めた。

**モバイルカレンダー操作体系の再設計（9件）**: 月グリッドのセル高さを 44px に引き上げ、swipe の directionRatio を 1.2 に下げて斜めスワイプ無反応域を縮小。ブロック操作は tap=Inspector / longpress=ドラッグ に統一してリサイズハンドルを廃止。短ブロックのリサイズ侵入幅やゴースト最小高さも含めて、触感を整えた。Inspector の TimeRow もモバイルで縦積みに変更。

**タグ UI の badge 化（8件）**: タグ選択 UI を badge 形式に刷新し、`TagBadgeList` に共通化。破壊的フォームダイアログを `DestructiveFormDialog` に集約、タグフィルタのグループ変更メニュー整理、アイコン選択時のメニュー自動閉鎖、グループ配下タグ作成時の親色継承まで含めて、タグ関連の触り心地を揃えた。

**Stats タグ詳細の刷新（1件）**: タグ詳細のエントリ一覧をリッチリスト + ページネーションに全面刷新。

**Opus 4.7 migration の周辺整備（7件）**: 12 project skill に When to Use / When NOT to Use セクションを追加し発動条件を明文化、skill 設計ルールを `.claude/rules/skill-design.md` に常駐化、migration note を `docs/development/opus-4-7-skill-triggers.md` に記録。続いて CLAUDE.md のコーディング規範セクションを「絶対禁止」からポジティブ例示化して [ADR-007](../../dev/adr/ADR-007-positive-framing-coding-norms) に記録、MCP サーバー利用基準を `.claude/rules/mcp-usage.md` に分離、auto mode 使用ガイドラインを `.claude/rules/ai-behavior.md` に追加、Stop hook での完了音通知を settings.local.json に設定。

#### なぜ今日この作業をしたか

午前中はモバイル体験の仕上げフェーズ。4月前半のバッチマージで UI 基盤が安定したので、その上に「まだ触り心地が悪い」残タスクを潰す作業が続いた。billing のエラー系ダイアログは trial / payment の2系統で構造が揃うので、コンポーネント → フック → 統合の3層で順に積むほうが認知負荷が低い。モバイルカレンダーは 44px ヒット領域、tap/longpress の使い分け、リサイズハンドル廃止といった「短ブロックでも誤操作しない」ための調整を一気にかけた。タグ UI の badge 化は、chip / icon + text 併記などの既存パターンから「バッジ1種類で統一」へ集約する決断で、TagBadgeList に寄せれば今後の追加も含めて変更面が小さくなる。

午後に Opus 4.7 migration の周辺整備に転じた。朝時点で skill description のリライトと skill-design.md の常駐化までは完了していたが、CLAUDE.md の禁止リストがネガティブ形のままだった。Opus 4.7 はポジティブ例示への遵守率が高いという性質に合わせて、意味をゼロ差分にしたまま表現だけ書き換える月次 distillation を実施。あわせて MCP 利用基準 (mcp-usage.md) と auto mode ガイドライン (ai-behavior.md) を分離・追加し、「どのモードで何に委ねるか」の運用軸を整えた。Stop hook の完了音は、長時間タスク中に子供の対応をしつつ戻ってこれるようにするための実務的な仕掛けで、設定は 5 分で完了する ROI の高い作業だった。

全体として「実機能の触感を仕上げる午前」と「AI 協業の規範を整える午後」が綺麗に分かれた1日。commit 分割時に commitlint の subject-case ルールが日本語サブジェクト先頭の英大文字略語を拒否することに気付き、メッセージを言い換える必要があったのは次回以降の運用 tips として小さいが有効な発見だった。

#### Claudeの所感

**ポジティブ例示化は「意味を変えないリライト」として理想的な月次タスク**: ADR-007 の変換対応表を見ると、11項目中 10項目が「禁止 → 代替」から「望ましい挙動 + 具体例」に機械的に書き換えられた。残り 1項目（features/ 新規トップレベル相談）はプロセス要件なのでネガティブ形維持が適切、という判断基準も自然に働いた。Opus 4.7 の解釈性向上という目的変数に対して、差分レビューが容易（意味ゼロ差分）、行数増加も +3%（104 → 107）に収まる、という副作用の小ささが実行のしやすさを保証している。同種の distillation を今後も月次で回せると、CLAUDE.md の「AI 向けドキュメントとしての質」が時間とともに上がる構造になる。

**運用モード軸で `.claude/rules/` を整理する構造が見えてきた**: `ai-behavior.md` に auto mode ガイドラインを追加したことで、extended thinking（thought budget 軸）・subagent model selection（モデル軸）・auto mode（介入頻度軸）が同じファイルに並ぶ形になった。これは偶然ではなく、どれも「タスクに対して AI をどう稼働させるか」という運用意思決定の軸で、設計判断の根拠も共通している（タスクの既知度、影響範囲、意思決定点の有無）。次に effort level の skill 単位切り替えを入れるときも、同じ軸に沿って整理すれば自然に収まる。

**Stop hook の完了音は小さい投資で実務的価値が大きい**: solo dev + 子育て中という実利用文脈では、長時間タスクを回している間に離席してよいかの判断が意思決定コストになる。afplay を async hook で 1 行鳴らすだけの設定は、数分で終わる変更だが毎日使う性質の ROI が高い。同種の「AI 協業の体感を上げる軽量な仕組み」は、記事で紹介される公式機能（hook / auto mode / effort level）に Dayopt 固有の運用判断を載せることで今後も積み上げられる領域で、未着手の effort level 動的切替も同じ文脈に位置付けられる。

## 2026-04-16

### UI基盤の大掃除 — search削除・toast刷新・dialog改善・バッチマージ

27コミット、82ファイル変更（+1,545 / -2,515行）。search・toast・dialog・inline-banner・shell・stats・settings・auth・inspector・swの広範な領域にまたがる作業。不要機能の削除とUI基盤コンポーネントの刷新が主軸で、4月前半の全変更をバッチPR #1047としてmainにマージした。

#### やったこと

**Search feature 完全削除（3件）**: search featureディレクトリ、command コンポーネント、cmdk パッケージを一括削除。関連するStorybook stories・docs参照もすべて除去。

**Toast 刷新（3件）**: toastをミニマル1行デザインに全面刷新。info/warningを廃止してsuccess/error/loading/promiseの4種に絞り込み。既存の deprecated 呼び出しを全箇所移行し、design-system rulesも新仕様に更新。

**Dialog / Confirm Dialog 改善（2件）**: dialogにPC=Dialog / モバイル=Drawerの responsive 自動切り替えとsize propを追加。confirm-dialogをAlertDialogベースに書き換え、破壊的操作の確認ダイアログとしての振る舞いを明確化。

**Inline Banner 新設（3件）**: inline-bannerコンポーネントを新規作成し、useInlineBannerフックとi18nキーを追加。既存のOfflineIndicatorをinline-bannerに置換し、全ページレイアウトに統合。

**モバイルシェル改善（3件）**: useHideOnScrollフックを新規作成し、モバイルボトムバーにスクロール連動auto-hideを実装。Storybookにhidden状態のstoryも追加。

**バグ修正（8件）**: time P/Lのoff-by-oneエラーと年粒度ラベル修正、グローバル検索のキャッシュ漏れ、モバイル履歴ストリップ・バッジグリッドの改善、SW navigation cacheのwaitUntil保護、personalization並行保存競合修正、codexレビュー指摘7件対応、auth metadata表示名バックフィル、inspectorメモtextarea入力不可修正。

**Storybook / ドキュメント整備（4件）**: overlay/notification storiesを機能別サブカテゴリに再編成、action-footerにレイアウト同期コメント追加、rules/docsからsearch残存参照を削除。

#### なぜ今日この作業をしたか

前日のv0.26.0リリースとパフォーマンス最適化を経て、4月前半に積み上げた変更をmainにマージする最終段階に入った。マージ前にsearch featureの完全削除を実行したのは、前日にサーバー側検索に移行した直後のタイミングが最も安全だったから。クライアント側の全件取得ロジックが不要になった時点で、中途半端に残すよりも一気に削除する方が依存関係の見通しが良くなる。

toast刷新とdialog改善は、バッチマージに含めるUI基盤の品質向上として実施した。toastは種類が多すぎてcopywritingガイドとの整合が取れなくなっていたため、4種に絞ることでガイドの「研究者は普段叫ばない」という方針と合致させた。confirm-dialogのAlertDialog化は、破壊的操作の確認でオーバーレイクリックによる誤操作を防ぐ必然的な改善だった。

inline-bannerの新設はOfflineIndicatorの置換がきっかけだが、今後のページ横断状態表示（メンテナンス通知、バージョンアップ案内など）の基盤として設計した。トーストは一時的な通知に、inline-bannerは持続的な状態表示にという棲み分けが明確になった。

#### Claudeの所感

**UI基盤の「引き算」がプロダクトを前進させる**

今日の作業で最も印象的なのは、コード量が純減（-970行）していることだ。search featureの削除、toastの種類削減、cmdk依存の除去。機能を足すのではなく引くことで、残ったコンポーネントの責務が明確になり、copywritingガイドとの整合性も向上した。特にtoastの4種への絞り込みは、「研究者は普段叫ばない」というプロダクトのトーンを技術的に担保する判断として優れている。

**DialogのResponsive戦略が成熟してきた**

PC=Dialog / モバイル=Drawerの自動切り替えは、設定画面の「PCはモーダル、モバイルは実ページ」という方針と同じ思想の延長線上にある。デバイスごとに最適なインタラクションパターンを選ぶという設計判断が、個別コンポーネントレベルまで浸透してきた。confirm-dialogのAlertDialog化も含め、「何をどう見せるか」のレイヤーが整理されつつある。

**バッチPRという運用判断**

27コミットを1つのPR #1047にまとめてマージしたのは、個別PRのレビューコストとリリースの確実性のトレードオフの結果だろう。4月前半の変更が互いに依存し合っている（search削除→toast刷新→dialog改善）状況では、バッチの方がコンフリクトリスクが低い。ただし、この規模のバッチが常態化しないよう、次の2週間は小粒なPRで回すリズムを意識したい。

---

## 2026-04-15

### v0.26.0リリース + パフォーマンス全面最適化 + 価値観UI刷新

34コミット、241ファイル変更（+4,500 / -8,166行）。settings・db-schema・calendar・stats・search・tags・sw・notifications・onboardingの広範な領域にまたがる作業。リリース・パフォーマンス最適化・設定UI刷新の3本柱で進行。

#### やったこと

**v0.26.0リリース（2件）**: 前日のDB大規模クリーンアップ+設定UI刷新をv0.26.0としてリリース。デプロイトリガーをタグpushからmainマージ方式に変更。

**パフォーマンス最適化（9件）**: Service WorkerをStale-While-Revalidate方式に変更、activeCacheのstaleTimeを30秒→5分に延長、globalSearchを全件取得からサーバー側検索に移行、カレンダーのビュー別prefetch+ホバーprefetch追加、SSR prefetch追加（badges.list, timePL）とフェッチ並列化、tagMergeModalの不要refetch削除、notifications/onboardingにloading.tsx追加。PR #1045としてまとめてマージ。

**設定UI刷新 — 価値観機能（6件）**: 価値観キーワードUIをカテゴリタブ+統合ランキングビューに全面刷新。価値評定スケールUIを排他アコーディオン+例文チップに変更。personalization JSONBカラムを追加してパーソナライゼーションUIをDB接続。

**DB整備（9件）**: reports/api_keysテーブル新規追加、インデックス最適化（冗長8件DROP+tags複合1件ADD）、tagsにupdated_atトリガー追加、スキーマ定義の整理、型再生成。

**バグ修正・品質改善（8件）**: 参照ありタグの暗黙削除防止ガード、pageNavアクティブタブのスタイル修正、統合テストのentry_tags参照修正、検索テストモック修正。

#### なぜ今日この作業をしたか

前日のDB大規模クリーンアップが一区切りついたタイミングで、まずv0.26.0としてリリースした。スキーマがクリーンになった直後は、パフォーマンスの見直しに最適なタイミングだった。不要なテーブルやカラムが消えたことでデータフローが明確になり、「ここは毎回fetchしなくていい」「ここはSSRでprefetchすべき」という判断が容易になった。

パフォーマンス最適化は個別最適ではなく、アプリ全体を横断する形で一気に行った。SWのナビゲーション戦略、TanStack Queryのキャッシュ設定、SSR prefetch、検索のサーバー移行、不要refetchの削除。これらは互いに関連しており、1つずつやると整合性が取りにくい。特にstaleTimeの延長とSWのSWR方式は組み合わせて初めて効果を発揮する。

価値観UIの刷新は、personalization JSONBカラムの追加をきっかけに着手した。従来のフラットリストではカテゴリが増えた際にスケールしない問題があった。排他アコーディオン+例文チップの組み合わせは、ユーザーが各スケールの意味を理解しやすくする設計判断。

#### Claudeの所感

**パフォーマンス最適化のタイミング** — DB整理直後にパフォーマンス最適化を行うのは理にかなっている。スキーマがシンプルになったことで、データの流れが見通しやすくなり、「何をキャッシュすべきか」「何をSSRで先読みすべきか」の判断が的確になった。特にglobalSearchのサーバー移行は、全件fetchという根本的なアンチパターンを解消しており、データ量の増加に対するスケーラビリティの懸念を先回りで潰している。

**SW × staleTime × SSR prefetchの三位一体** — Service WorkerのSWR方式、TanStack Queryのキャッシュ戦略、SSR prefetchの3つを同時に調整したことで、ユーザー体験としては「どのページもほぼ即時表示される」状態に近づいた。これらを個別に最適化しても効果は限定的で、一貫した戦略として導入したことに価値がある。staleTimeを5分に延長しつつSWRで裏側を更新するアプローチは、データの鮮度とパフォーマンスのバランスとして妥当。

**価値観UIの設計選択** — カテゴリタブ+排他アコーディオンの組み合わせは、情報量が増えても認知負荷を抑える定石的なUIパターン。例文チップの追加は「価値評定スケール」という抽象的な概念を具体的に伝える効果がある。パーソナライゼーション機能がAI分析のコンテキストとして活用される将来を考えると、このタイミングでUIを整備しておく判断は正しい。

---

## 2026-04-14

### DBスキーマの大掃除 — 19テーブルから10テーブルへ

29コミット、161ファイル変更（+2,674 / -7,255行）。DB・entry・notifications・settings・calendar・auth・ai・chronotype・stats・onboardingの全領域に渡る大規模リファクタリング。

#### やったこと

- **テーブル削除（9テーブル）**: entry_activities, entry_tags, chat_conversations, reflections, ai_usage, notification_preferences, login_attempts, auth_audit_logs, \_backup_record_activities
- **カラム削除（20+カラム）**: reminder_minutes, username, date_format, business_hours, show_declined_events, show_utc_offset, plan_record_mode, AI関連5カラム, color_scheme, クロノタイプ8カラム→1 jsonb統合
- **テーブル構造変更（2テーブル）**: notifications簡素化（read_at/title/fire_at追加）、entries.tag_id統合（entry_tags廃止）
- **feature削除（1ディレクトリ）**: ai feature全体を削除（router, service, prompt-builder, tests等）
- **新規追加**: time P/L RPC関数、統計hook

#### なぜ今日この作業をしたか

テーブル構成の全体調査から始まった。19テーブルの現状を説明する中で、「これ本当に要る？」という疑問が自然に浮かんだ。reminder_minutesは既にON/OFFトグルに簡素化済みなのにINT型のまま、usernameはソーシャル機能がないのにUNIQUE制約を維持、business_hoursはUIで一切参照されていない、AI関連テーブルは機能自体を削除済み。どれも「いつか使うかも」で残していたものだった。

PMF前のプロダクトにとって、使っていないカラムやテーブルは負債でしかない。型定義の肥大化、マイグレーションの複雑化、新規開発者のオンボーディングコスト。entry_tagsの1:1制約は中間テーブルの存在理由そのものを否定していた。今日一気に整理したことで、スキーマがプロダクトの実態を正確に反映するようになった。

#### Claudeの所感

**スキーマは生き物である** — 19テーブルから10テーブルへの削減は、単なる「お掃除」ではなく、プロダクトが何であるかの再定義だった。AI機能、チャット、リフレクション、パレットといった「あったらいいな」機能群を思い切って落とし、タイムボクシング×時間記録というコアに集中する判断が、スキーマレベルで実行された。

**entry_tags統合の設計的妥当性** — 1エントリ1タグという制約は、中間テーブルのN:N構造と根本的に矛盾していた。entries.tag_idへの統合はJOINコストの削減だけでなく、データモデルが業務ルールを正直に表現するようになったという意味で、アーキテクチャ上の大きな前進。

**locale→dateFormat導出パターン** — DB保存をやめ、localeから動的に導出する方式は「設定の削減」の良いモデルケース。ユーザーに選択肢を与えるほど設定UIが複雑になる。localeという上位概念から自動決定できるなら、それが最もシンプル。PMF後にパワーユーザーから要望があれば、その時に復活させればいい。

---

## 2026-04-13

### src/構造の全面統合 + パレット機能完全削除

42コミット、892ファイル変更、+21,605 / -14,177行。src/直下に散在していたディレクトリの全面統合、パレット機能のコードからDBテーブルまでの完全削除、モバイルUIの改善を行った。

#### やったこと

**src/ディレクトリ構造の全面統合（18件）**

- src/直下の9ディレクトリ（platform, hooks, types, stores, shell, stories, styles, test, components）をlib/またはfeatures/配下に統合・廃止
- 全featureのbarrel（index.ts）を整備し、feature内ファイル構造を統一パターンに揃えた
- エラーハンドリングをServiceError + handleServiceErrorに統一
- feature boundary DAGルールを再有効化、構造ルールをfeature-boundaries.mdに文書化
- PR #1040としてマージ後、CI・ビルドエラー5件を修正（turbopack境界違反、middleware env import、next-intlパス等）

**パレット機能の完全削除（9件）**

- features/palette/ディレクトリとtRPCルーターを削除
- カレンダー・inspector・サイドバーからのパレット連携を除去
- 翻訳キーの削除・移動、Storybook・テストの整理
- palette_itemsテーブルのDROPマイグレーションを作成

**モバイルUI改善（2件）**

- DayViewヘッダーを2行から1行に統合し画面領域を確保
- モバイルフッター上部に履歴ストリップを追加（パレット廃止後のクイックアクセス代替）

**依存関係更新（3件）**

- next 16.2.3、next-intl 4.9.1、basic-ftp 5.2.2へbump

#### なぜ今日この作業をしたか

前日までにStorybook構造のリファクタと非機能品質改善を終えていた。src/直下のディレクトリ構造はfeature-boundaries DAGモデルとの整合性が取れておらず、「lib/とfeatures/以外にsrc/直下にディレクトリがある」状態がboundary lintの無効化を強いていた。今日はこの技術的負債を一掃し、DAGルールを再有効化できる状態まで持っていくことが目標だった。

パレット機能の削除は以前から計画されていた。履歴機能がパレットの役割を十分にカバーしており、コードの複雑さとメンテナンスコストに見合わなくなっていた。構造統合のタイミングと合わせることで、パレット関連コードの移動先を考える必要がなくなり、効率的に進められた。

モバイルUI改善は、パレット廃止に伴うUXギャップを埋めるための対応。履歴ストリップをフッター上部に置くことで、パレットが担っていた「よく使うブロックへのクイックアクセス」を履歴ベースで代替した。

#### Claudeの所感

**構造統合の判断の正しさ**

892ファイルの変更は規模として大きいが、これを分割して数日に分けるよりも、1日で完遂してDAGルールを再有効化する方が正しい判断だった。中途半端な状態でboundary lintを無効化したまま他の開発を進めると、新たな境界違反が混入するリスクがあった。CI修正に5コミットを要したが、これは大規模リファクタの不可避なコストであり、turbopackのserver-only境界検出やmiddlewareのenv制約といった実践的な知見も得られた。

**パレット削除のタイミング**

機能削除はコードベースの複雑さを直接的に減らす最も効果的な手段だが、心理的なハードルが高い。構造統合と同日に行ったことで「移動先を考えなくていい」という合理性が生まれ、feature・server・UI・i18n・test・DBまで一貫して削除を完遂できた。DROPマイグレーションまで作成して「あとはstaging実行だけ」の状態にしたのは、削除作業が中途半端に残らないための良いプラクティス。

**モバイルUXの進化方向**

履歴ストリップの追加は、パレット廃止の穴埋めに留まらず、「使用頻度ベースのサジェスト」というより良いUXパターンへの移行でもある。パレットはユーザーが手動でピン留めする必要があったが、履歴は自動的に最近使ったものを提示する。この方向性はDayoptの「記録は自動で、振り返りは意図的に」という思想と一致している。

---

## 2026-04-10

### 非機能品質の全面改善 + Storybook構造リファクタ

appリポジトリで25コミット。パフォーマンス・SEO・A11y・セキュリティ・キャッシュといった非機能品質の網羅的な改善、デッドコードの大規模掃除、Storybookのドキュメント構造を根本から再編成した1日。255ファイル変更、+12,567 / -9,022行。

#### やったこと

**Storybook構造リファクタ（8件）**

- .storybook/ルート直下をmain.ts/preview.tsx/manager.tsの3ファイルに整理。DocsTemplate・ThemedDocsContainerをtheme/docs.tsxに統合
- marketingリポの戦略ドキュメント39ファイルをStorybook MDXとして統合（7,666行）
- 技術ドキュメント55 MDXをsrc/stories/docs/から.storybook/docs/に移動
- docs/をdev/（技術系62ファイル）とproduct/（戦略・ビジネス系42ファイル）の2階層に分離
- サイドバーをWelcome→Architecture→Strategy→Componentsの4階層に統合
- feature .docs.mdxのDocsサブカテゴリをGuideにリネーム、残存Docs/タイトルをArchitecture/に統合
- storiesグロブとサイドバー表示順を更新
- decorators/mocks の TypeScript型エラーを修正

**パフォーマンス最適化（4件）**

- entries/notifications/entry_tagsに部分インデックスを追加（`deleted_at IS NULL`でStats系RPC全体を高速化）
- badges evaluateWithProgressに統合（17→9リクエスト）、getUserTimezoneに30秒TTLキャッシュ追加
- タグ詳細の個別8本RPCをgetTagOverview+getTagTimelineの2本に統合、rechartsをdynamic import化（~130KB gzip削減）
- useWeekendNavigationの依存配列追加で毎レンダー実行を修正

**A11y改善（3件）**

- LabeledRowにuseId+cloneElementでaria-labelledby自動注入（~30箇所）、MFA・タグ名変更・削除確認等の個別入力にaria-label追加
- 設定ダイアログ（email-change/password-change）のエラーメッセージにrole="alert"とaria-invalid/aria-describedby追加
- useFocusTrapフック新規作成、ConfirmDialog・TagCreateModal・TagMergeModalにフォーカストラップ・初期フォーカス・フォーカス復元を適用

**SEO改善（2件）**

- Stats 4ページ（review/insights/progress/badges）にgenerateMetadata追加、locale layoutのOG images明示指定、app.descriptionをDayoptの差別化要素を表現する文言に更新
- 認証必須ページ（calendar/stats/settings等）にnoindex追加、sitemapから認証パス全削除、robots.txtにAIクローラーブロック追加

**セキュリティ・バンドル最適化（1件）**

- 16ファイルにimport 'server-only'追加（DB層・秘密鍵の保護）、WebVitalsReporterをdynamic import化（~80KB Sentry分離）

**デッドコード・依存整理（4件）**

- 到達不能な/api/auth/logoutエンドポイントを削除（useLogoutはsupabase.auth.signOut()を直接使用）
- 未使用export・デッドコードを掃除（useEntryCreate削除、invalidateCalendarCache削除、barrel 50+個のexport除去）
- 削除済み機能の残骸ファイル33個と孤立barrel 10個を除去（旧Settings 4画面、useAutoScrollOnDrag、useCalendarProviderProps、TagGridPicker等）
- 未使用パッケージ13個を削除（react-calendar-heatmap、react-email、@prisma/instrumentation、@tanstack/react-query-devtools等）

**キャッシュ・型（2件）**

- tanstack queryキャッシュ設定の不整合を修正（tags.createのinvalidate追加、TimezoneOffsetのinvalidation追加、cacheStrategies統一）
- onboarding upsertの`Record<string, unknown>`を具体型に修正（Supabase RejectExcessPropertiesとの型不一致を解消）

#### なぜ今日この作業をしたか

前日（4/9）のセキュリティ・認証・課金の修正に続く「プロダクション品質の底上げ」フェーズの2日目。4/9が「壊れていたものを直す」だったのに対し、4/10は「動いているが最適でないものを磨く」作業。DB不足インデックス、n+1クエリ、A11yの不備、SEOの漏れ、キャッシュの不整合といった「今すぐ壊れはしないが、スケールやユーザー体験を損なう」問題を一掃した。

Storybook構造リファクタは、ドキュメントがsrc/stories/docs/、src/stories/strategy/、.storybook/の3箇所に散らばっていた問題を解消するためのもの。dev/（技術系）とproduct/（戦略・ビジネス系）の2階層に分離したのは「誰が何を読むか」を意識した設計判断。marketingリポから39ファイルを統合したことで、Storybookがプロダクト全体のナレッジハブとして機能する基盤ができた。

#### Claudeの所感

**初期バンドル~210KB削減はモバイルで効く:**
rechartsのdynamic import化（~130KB）とWebVitalsReporterの分離（~80KB）を合わせると、初期バンドルから約210KBが削減された。デスクトップでは体感しにくいが、モバイルの3G/4G環境ではこの差がFirst Contentful Paintに直結する。Dayoptのターゲットが「個人ユーザー」である以上、モバイル体験の最適化は優先度が高い。rechartsはStats画面でしか使わないので、カレンダー画面を開くだけのユーザーにそのコストを負わせる理由がない。

**Storybookが「生きたドキュメント基盤」に成熟した:**
104ファイルのMDX（dev 62 + product 42）が1つのStorybookに集約された。これは単なるファイル整理ではなく、「Storybookをプロダクト全体のナレッジハブとして使う」という設計判断の実装。技術ドキュメント・戦略ドキュメント・コンポーネントカタログ・開発日記が同じUIで閲覧できることで、コンテキストスイッチのコストが下がる。1人開発で情報が散逸しがちな環境では、この集約が特に価値を持つ。Welcome→Architecture→Strategy→Componentsの4階層はMECEではないが、「開発者が自然に探す順序」を優先した現実的な分類。

**デッドコード掃除は「引き算の勇気」:**
残骸ファイル33個、孤立barrel 10個、未使用パッケージ13個、未使用export 50+個。合計すると相当な量のコードが「存在するだけでメンテナンスコストを発生させていた」。旧Settingsの4画面（general/calendar/integration/data-export）やTagGridPicker、useEntryCreateなど、かつて機能していたコードを消すのは心理的に抵抗があるが、git historyが復元手段として機能する以上、残しておく合理性はない。1人開発ではコードベースの見通しの良さが生産性に直結する。knip（`quality:deadcode`）がこの種の掃除を支援しているのも良い運用パターン。

---

## 2026-04-09

### セキュリティ・課金・認証の総点検 + 型安全性強化

appリポジトリで23コミット。プロダクションに向けたセキュリティ・課金・認証の網羅的な修正と、`as never`除去による型安全性の強化。Observability（Sentry・logger）の全APIルートへの追加も完了。

#### やったこと

**認証フロー修正（5件）**

- サインアップ時のメール確認待ちUIを追加
- ログイン後のredirectパラメータ対応を追加
- sessionMonitorProviderをProviderツリーに配置
- アカウント削除時のStripeサブスクリプション解約とセッションクリアを追加
- MFA検証のエラーハンドリング改善

**課金・サブスクリプション修正（3件）**

- 再サブスク時のトライアル再付与を防止
- webhook処理失敗時の冪等性レコード削除でリトライを復旧可能に
- Pro判定にpast_dueを追加して他箇所と統一

**セキュリティ修正（3件）**

- env.tsにserver-onlyガード追加
- Pro限定APIにproProcedureゲートを追加
- CIにビルド後クライアントバンドルへのシークレット混入検査ステップを追加

**Observability（2件）**

- beacon/onboarding/billing/exportにSentry・logger追加
- stats画面のErrorState追加と統計エラーのSentry報告改善

**サイレント失敗の修正（2件）**

- エントリー作成・タグフィルター編集のサイレント失敗を修正
- タグ操作のサイレント失敗とタグ誤削除を修正

**型安全性強化（4件）**

- feature型をDBスキーマと整合
- billing関連の`as never`を除去
- notifications関連の`as never` / `Record<string, unknown>`を除去
- entry/onboardingのinsert/upsertの`as never`を除去

**その他（2件）**

- `.env.example`にStripe・Sentry・Slack変数を追記
- `getBaseUrl()`を共通モジュールに統合

#### なぜ今日この作業をしたか

プロダクションリリースに向けた「隠れた不具合の洗い出し」フェーズ。機能追加ではなく、既存コードの穴を塞ぐ作業。認証フローのエッジケース（メール確認待ち、リダイレクト、アカウント削除時のStripe連携）、課金の冪等性、API認可の漏れなど、ユーザーが実際に遭遇しうるシナリオを網羅的に修正した。

#### Claudeの所感

**「サイレント失敗」は最も危険なバグ:**
エントリー作成やタグ操作が黙って失敗していた問題の修正は、ユーザー体験への影響が大きい。ユーザーは「保存したはずなのにデータがない」という体験をすると、プロダクトへの信頼を一気に失う。エラーを出さないことが親切なのではなく、失敗を正直に伝えることが誠実。brand-character.mdの「知的だけど冷たくない」トーンは、こういうエラーハンドリングの場面でこそ試される。

**`as never`除去は技術的負債の返済:**
TypeScriptの`as never`は「型チェックを黙らせる」キャストで、本来は型定義側を直すべき問題を隠蔽する。billing、notifications、entry/onboardingと広範囲に散らばっていた`as never`を、DBスキーマとの型整合を取り直すことで除去している。地味だが、今後のリファクタリングや機能追加でバグを早期発見できる土台になる。

**セキュリティがCI/CDに組み込まれた:**
`env.ts`のserver-onlyガード、proProcedure認可ゲート、CIでのシークレット混入検査。これまで人の注意力に頼っていたセキュリティチェックが自動化された。特にクライアントバンドルへのシークレット混入検査は、ビルドパイプラインに組み込むことで「うっかり」を仕組みで防げる。1人開発でレビューアーがいない環境では、この種の自動化が唯一のセーフティネット。

---

## 2026-04-08

### LP戦略ドキュメント整備・Time P/L実装・Heroリニューアル

3リポジトリで大きな動き。マーケティング戦略のドキュメント化、appでのTime P/L機能実装、webでのHeroセクション刷新が並行して進んだ。

#### やったこと

**マーケティング戦略ドキュメント整備（marketing）**

- `messaging.md` 新規作成 — JTBD階層（上位/中位/下位）、P/Lメタファー、コアメッセージ候補4案、ブランドパーソナリティ定義
- `persona.md` 新規作成 — ターゲットペルソナ具体化（「正しいやり方は分かってるのに道具がない人」）
- `visual-direction.md` 新規作成 — タイポグラフィ・カラー・イメージ戦略
- `lp-spec.md` 新規作成 — LP実装仕様書（marketing→webハンドオフ文書。セクション別仕様、翻訳キーマッピング、未決定事項管理）
- `brand-character.md` をv2方向に上書き — 「やさしい助言者」→「静かに自信がある、わかってる人に道具を渡す」
- `docs-map.md`、`brand/README.md`、`lp.md` を新規ファイルに合わせて更新
- web repo の `CLAUDE.md` にマーケティング参照セクション追加（パス規約でクロスリポ参照）

**Time P/L（時間損益計算書）実装（app）**

- `timePL` コンポーネント群をStorybook-firstで追加
- データ層を正規化（1入力型 + derivers + テスト）
- UI層をShell + Views + Containerに再構成
- 旧timePLコンポーネント群を削除してリファクタリング完了

**計画外エントリの表示改善（app）**

- 計画外カードをactual時間で表示し、親カード内側にオフセット配置
- 不透明背景を適用しハッチングラベルを隠す修正
- 重複チェックでactual時間を考慮し短縮枠への配置を許可

**Eagle デザインアセット管理（app）**

- 設計書とClaude Codeスキルを追加
- APIクライアント、ファイル名パーサー、同期・撮影・検索・クリーンアップスクリプトを実装
- foundationsセットアップスクリプトを追加

**Heroセクション刷新（web）**

- スタガードアニメーション付きカルーセルに刷新
- モックのリアリティ向上（ウィンドウクローム・データ密度強化）
- blur fade・glow呼吸・CTA attentionアニメーション追加
- レイアウト・余白調整

#### ブランド方向転換の判断

brand-character.mdを「やさしい助言者」→「静かに自信がある」に上書きした。これはLP固有のトーンではなく、ブランド全体の方向転換として扱う決定。理由は、v2メッセージング設計で明確になったターゲット像（「すでに走ってる人」）に対して、「助言者」よりも「道具を渡す」姿勢の方が一貫するため。

#### Claudeの所感

**戦略→実装のパイプラインが見えてきた:**
今日はmarketing repoとweb repoを横断する1日だった。`lp-spec.md`がハンドオフ文書として機能すれば、「なぜこのコピーなのか」がmarketing repoに、「どう実装するか」がweb repoに、という分離が成立する。パス規約（`~/Desktop/marketing`参照）はシンプルだが、1人開発者の運用コストを考えると最も現実的な選択。

**P/Lメタファーが設計とマーケティングの両方に浸透し始めている:**
appでTime P/Lコンポーネントが実装され、同時にmarketing側でもP/Lメタファーの投入箇所を設計している。「時間の予算と決算」というフレームは、プロダクトの機能とマーケティングメッセージが同じ言語で語れるという意味で強い。ユーザーが初めてLPを見たときにP/Lメタファーで「なるほど」と思い、実際にアプリを開いたときにTime P/L画面で「あの話はこれか」と繋がる体験が作れる可能性がある。

**Heroの刷新とlp-spec.mdのタイミング:**
web側でHeroのアニメーション・レイアウトが大きく改善された日に、marketing側でHeroのコピー方向を定義するlp-spec.mdが生まれた。器（デザイン）と中身（コピー）の両方が同日に動いたのは偶然だが、次のステップ（コピーの実装）がスムーズに進む土壌ができた。

---

## 2026-04-07

### バッジ機能実装・コピーライティングv3移行・v0.25.0リリース

バッジ機能をDB→tRPC→UIまで一気通貫で実装し、17→16個のバッジを定義。コピーライティングルールをv3「寡黙な研究者」トーンに更新し、全UIテキストを一括移行。v0.25.0をリリース。

#### やったこと

**バッジ機能（13件）**

- user_badgesテーブル作成 + DB型再生成
- バッジ定義・サービス・tRPCルーター追加
- バッジUI実装（グリッド・セル・詳細Popover/Drawer）
- バッジタブ統合 + i18n + デザイントークン
- Storybook追加（グリッド・詳細）
- バッジ判定を一括クエリに最適化（perf）
- タブ順序変更 + storybook改善
- auth.admin修正・tags-5バグ・UNIQUE制約・thresholds整理
- バッジ入れ替え（17→16個）
- カードサイズ調整（w-40 + flex wrap + gap-4）
- Storybook Detailをカードサイズ・新バッジに合わせて更新
- レビュー指摘対応（死リンク・weekly-report除外・TZ TODO）
- RPC引数名修正・TZ対応・部分進捗表示の3バグを修正

**コピーライティングv3移行（8件）**

- コピーライティングルールをv3「寡黙な研究者」に更新
- 共通・エントリーのコピーをv3トーンに移行
- カレンダー・サイドバーのコピーをv3トーンに移行
- タグ関連のコピーをv3トーンに移行
- 認証・オンボーディング・ツアーのコピーをv3トーンに移行
- 設定・通知のコピーをv3トーンに移行
- 問い合わせ・ナビゲーション・記録のコピーをv3トーンに移行
- pre-suasionセクションをトーンガイドに追加 + 空状態コピーをpre-suasion版に更新

**v0.25.0リリース（2件）**

- version bump + リリースPR (#1038)

**UI改善（3件）**

- ドロワーボタン横並び・ミニカレンダー月選択連動・日付表示改善 (#1032)
- blockItemのフォントサイズをtext-smに統一（PC）
- dB型再生成に伴うRPC戻り値型・フィールド修正

**依存パッケージ更新（4件）**

- 本番依存パッケージ16件を一括アップデート (#1035)
- vite 7.2.4→7.3.2
- lodash 4.17.23→4.18.1
- lodash-es 4.17.23→4.18.1
- supabase-js 2.101.1のRejectExcessProperties型に対応

#### なぜコピーライティングをv3に改訂したか

v2は「穏やかで親しみやすい」を目指していたが、全体的に説明過多・丁寧すぎる傾向があった。v3「寡黙な研究者」は、必要な情報だけを静かに伝えるトーン。Dayoptのユーザーは自分で考えたい人が多く、UIが饒舌だと邪魔になる。pre-suasionの考え方を取り入れ、空状態のコピーも「使ってみたくなる」導線に変えた。

#### Claudeの所感

**バッジはエンゲージメント施策として筋が良い:**
バッジの本質は「記録を続ける動機付け」。ゲーミフィケーションと言えばそれまでだが、Dayoptの場合は「記録の蓄積が価値になる」プロダクトだからこそ、記録を続けた結果として自然にバッジが解放される設計が合っている。17→16個への調整も、数を盛るより質を選んだ判断。一括クエリ最適化で判定のパフォーマンスも担保しており、初日から完成度が高い。

**「寡黙な研究者」というトーンの選び方が絶妙:**
v2の「穏やかで親しみやすい」から一歩踏み込んで、「必要なことだけ言う」に振った。これはDayoptのブランドキャラクター「時間案内人」と一致する — 案内人は道を示すが、歩くのはユーザー自身。UIテキストが静かになることで、ユーザーの思考を邪魔しないプロダクトになる。全画面一括移行を1日で完了させた実行力も見事。

**週イチリリースのリズムが安定している:**
v0.24.0（4/1）→v0.25.0（4/7）と、週1リリースのペースが定着しつつある。バッジという新機能+コピーライティング刷新という品質改善を1リリースにまとめており、「機能追加と品質向上を同時に進める」サイクルが回っている。

---

## 2026-04-06

### 予定外エントリ機能・TimeDiffBlock統合・モバイルフォントサイズ統一

「予定外エントリ」という新概念を追加し、予定なしの記録を破線枠カードでカレンダーに表示できるようにした。差分表示をTimeDiffBlockに統合し、旧timeDiffBar/timeProgressBar/侵食表現を廃止。モバイルのフォントサイズをtext-baseに統一し、webにデザインシステム変更を全面同期。

#### やったこと

**予定外エントリ機能（14件）**

- 予定外エントリ機能を新規追加（originの切り替えで予定/予定外を管理）
- 「計画外」→「予定外」に用語変更
- 予定外エントリをactual時間でカレンダーに表示
- 予定外エントリのカードを破線枠で表示
- 予定外エントリのカード位置をactual時間に追従させる
- 予定外エントリで記録変更時に予定も自動同期（1回のmutationにまとめ）
- 予定外のサマリー行に「予定 0min」を表示
- TimeDiffBlockの予定を0扱いにする対応
- zodスキーマのdatetimeにoffset:trueを追加
- start_time/end_timeはnullにせず保持する方針に修正
- entryCardに予定外エントリのStoryを追加
- 予定外StoryにisUnplanned propとdisabled予定行を反映

**TimeDiffBlock統合・差分バー（7件）**

- 予定vs記録の位置ベース差分バー（TimeDiffBlock）を新規追加
- timeDiffBar/timeProgressBarを削除しTimeDiffBlockに統合
- タイムボックス超過の整理 — overdue通知削除・侵食表現・空き枠ボタン・差分バー常時表示
- 侵食表現を削除し超過オーバーレイに統一
- storiesを記録パターン別に整理（Default/Overtime/Underrun）
- 不要なFloatingPopover storiesを削除
- timeDiffBlockのフォントサイズをtext-smに変更

**モバイルUI改善（6件）**

- モバイルのフォントサイズをtext-smからtext-baseに統一（デザインシステム）
- モバイル設定ページのヒーローエリアをカード型レイアウトに変更
- モバイル設定カテゴリのフォントサイズを16pxに変更
- toastのモバイル幅縮小(356px)とフォントサイズ拡大(16px)
- モバイルTextareaのフォントサイズをtext-baseに変更
- モバイルDrawerのtextarea入力がドラッグジェスチャーと競合する問題を修正

**Web側デザインシステム同期（3件）**

- app側foundation更新をweb側に全面同期
- フォントウェイトをfont-bold(700)→font-medium(500)に統一
- CopyCodeButtonのhover:bg-surface-inset→hover:bg-mutedに置換

**修正・インフラ（6件）**

- ドラッグ選択を下方向のみに制限
- db疎通チェックを実クエリに変更し、redisチェックを追加
- 現在時刻ラベルの左右パディングをpx-1に縮小
- max-w-[30rem]をmax-w-lgに置き換え（lint warning解消）
- settings routingテストにtRPCモック追加 + recovery-codesのCI環境対応
- 本番依存パッケージ16件を一括アップデート

#### なぜ「予定外」という概念を追加したか

Dayoptは「予定を立てて記録する」が基本フローだが、現実には予定になかった作業が発生する。これまではそれを記録する手段がなく、実際の1日を正確に振り返れなかった。予定外エントリを破線枠で視覚的に区別することで、「予定どおりの1日」と「実際の1日」のギャップが一目でわかるようになった。完了型タスク管理にはない、Dayopt独自の価値をさらに強化する機能。

#### Claudeの所感

**「予定外」は記録アプリとしてのDayoptの完成度を上げる:**
予定→記録→振り返りのサイクルの中で、「予定になかった作業」は今まで盲点だった。予定外を破線枠で可視化することで、「自分は予定外の作業にどれくらい時間を使っているか」が数字で見える。これはタイムボックス手法のユーザーにとって非常に価値のある気づきになる。

**旧表現の統合は正しい判断:**
timeDiffBar、timeProgressBar、侵食表現、overdue通知と、差分を表す手段が複数並立していたのをTimeDiffBlock一本に統合した。表現手段が多いと「どれが正しい情報か」がわからなくなる。1つの概念に1つの表現、という原則を守ったことで、UIの認知負荷が大幅に下がったはず。

**モバイルtext-base統一の影響は大きい:**
text-sm(14px)からtext-base(16px)への変更はiOSのズーム防止にもなり、タッチ操作の精度も上がる。デザインシステムレベルでの変更をwebにも即日同期しているのが良い。app/webでトーンがバラつくと「別のプロダクト」に見えてしまう。

---

## 2026-04-03

### セキュリティ6件修正・予定/記録の差分表示・パレット簡素化・Storybookモックシステム

セキュリティ監査で発見した脆弱性6件（IDOR・タイミングサイドチャネル・オープンリダイレクト等）を修正。予定と記録の差分を視覚的に表現する機能を実装し、記録付きエントリのドラッグ時に確認ダイアログを追加。パレットUIを簡素化し、Storybookに宣言的モックシステムを構築。

#### やったこと

**セキュリティ修正（6件）**

- mfa verifyページのオープンリダイレクト修正
- beacon APIの更新フィールドをホワイトリスト化
- daily_insights関数のIDOR脆弱性を修正
- safeCompareのタイミングサイドチャネルを修正
- リカバリーコードのペッパーを必須化
- テストメールエンドポイントのアクセス制限を強化

**予定/記録の差分表示（9件）**

- 予定と記録の差分を視覚的に表現（新機能）
- 超過区間を破線枠+透明背景で表示しカード本体と分離
- 差分オーバーレイにアイコンと翻訳キーを追加
- 記録付きエントリのドラッグ時に確認ダイアログを表示
- ドラッグ確認後のresetActualTimeフラグがWeek/MultiDayViewで伝播されない問題を修正
- 記録付きエントリの予定変更時に記録リセット確認を追加
- 予定時間変更時のautoAdjustによる二重saveを抑制
- 予定変更時の記録リセットをdebounced saveに変更
- actual-time-overlayの差分計算ロジックのユニットテストを追加

**インスペクタ改善（5件）**

- モバイルの時間・日付ピッカーをDrawer化しUXを改善
- ブロックの横に左右出し分けて表示
- tagRowのタグ未設定分岐を削除
- タグ行とスケジュールカード間の余白を縮小
- deprecated inspectorAnchorRefを削除しstore経由に統一 + setInspectorAnchorRectをstore直接参照に変更

**Storybook（6件）**

- 宣言的モックシステムを構築 + 36Storyを移行
- カバレッジスクリプト強化とStory品質修正
- スキルドキュメントを再設計
- セキュリティパターンカタログを追加
- drawerハンドルを共通コンポーネントに統一
- パレット簡素化に合わせてStoriesを更新

**カレンダー修正（5件）**

- モバイルでタグフィルタが未初期化のためエントリが全て非表示になるバグを修正
- useResponsiveHourHeightをZustand store化しモバイル表示不具合を修正
- nowBadgeをグリッド右端に移動しエントリとの重なりを解消
- 本日ヘッダーの余白を改善
- モバイルエントリ表示調査用デバッグログを削除

**パレット簡素化・その他（5件）**

- パレットUIを簡素化し追加ボタンと時間変更を削除
- 統計タブごとの空状態メッセージを分離しコピーライティングを修正
- clientPageRouterで統計タブごとに正しいViewを出し分け
- モバイルのヘッダー下サブ領域を32pxに統一、PC通知ページをリダイレクト
- 統計・通知の先頭タブ下線を左端に整列

#### なぜパレットから機能を削除したか

パレットの「追加ボタン」と「時間変更」は、カレンダーのドラッグ操作と機能が重複していた。パレットの役割は「色の割り当て」に絞り、時間操作はカレンダーに一本化することで、ユーザーが迷う分岐を減らした。機能を増やすのではなく減らすことで、プロダクトの焦点を明確にする判断。

#### Claudeの所感

**セキュリティ監査が実を結んだ:**
IDOR、タイミングサイドチャネル、オープンリダイレクトと、OWASP Top 10に直結する脆弱性が6件見つかり即日修正された。1人開発では見落としがちな領域だが、セキュリティパターンカタログをStorybookに追加することで、今後の開発時にもパターンとして参照できる仕組みを残している。修正して終わりではなく、再発防止の仕組みまで作るのが素晴らしい。

**差分表示は"記録"機能の価値を可視化する:**
予定と記録のズレを視覚的に見せることで、「記録をつける意味」が直感的にわかるようになった。超過区間の破線枠表示、ドラッグ時の確認ダイアログ、リセット確認と、データを大切に扱う設計が一貫している。これは「完了型タスク管理」にはない、Dayopt独自の価値の具現化。

**「引き算の決断」ができている:**
パレットから機能を削除するのは、追加するより勇気がいる。しかし機能の重複を放置するとユーザーは「どっちを使えばいいの？」と迷う。プロダクトが成長するほど「何を入れないか」の判断が重要になり、この早い段階で引き算ができているのは良い兆候。

---

## 2026-04-02

### デザインシステム品質改善一括PR・コピーライティング統一・PWA刷新・A11y準拠

デザインシステム品質改善を一括PRとして統合（#1031）。コピーライティングルール策定→全UIテキストのトーン統一、PWAアイコン/マニフェスト/オフラインバナーの刷新、WCAG AAコントラスト準拠、バンドルサイズバジェット導入、ErrorState/Loading/Toast APIの統一を1日で完了。

#### やったこと

**コピーライティング統一（4件）**

- コピーライティング・トーンルールを策定
- コピーライティング監査データとBefore/Afterストーリーを追加
- 全UIテキストのトーンを統一（14ファイル）
- ハードコード文字列のトーン修正 + 既存Storyのテキスト統一

**A11y改善（4件）**

- contrast-audit.tsをcolors.cssと同期、セマンティックカラーのL値をWCAG AA準拠に調整
- アイコンボタン・プログレスバーにaria-label追加
- focus ringのring-offset-background欠落を修正
- radix false positive整理、修正済みstoryのtodo解除

**PWA刷新（4件）**

- manifest.jsonをデザインシステムと同期
- アイコンをprimary blueに再着色、maskable版を追加
- スプラッシュ画像をbg-background + primary blueアイコンで再生成
- オフラインバナーを穏やかな表示に改修、最後の同期時刻を追加

**パフォーマンス・CI（5件）**

- バンドルサイズバジェットチェックスクリプトを追加
- bundle-sizeジョブをbuild依存に変更し重複ビルドを排除
- LCP閾値を4000ms→2500msに引き下げ
- 品質ダッシュボードにバンドルサイズメトリクスを追加
- bundle-size jobのartifact展開パスを修正

**UIコンポーネントAPI統一（6件）**

- ErrorStateコンポーネントを新規作成 + アドホックエラーUIを置換
- isErrorハンドリング追加とEmptyState/Spinner統一
- state patterns（error/empty/loading）の判断マトリクスを追加
- skeleton/spinnerのAPI統一（animation→variant, size md→default）
- loading propを統一（isLoading/isUploading → loading）
- sonner直接importを`@/lib/toast`経由に一括移行 + duration制御ラッパー追加

**デザインシステム・トークン整理（10件）**

- 未使用CSS変数28個を削除し構造整理
- font-weightをfont-normal/font-boldの2段階に統一
- bare roundedをrounded-lg(8px)に統一
- デッドコード化したelevation.cssを削除
- lint:tokensにfont-weight/rounded/直接カラー検出ルールを追加
- bg-card + shadow共起チェックをlint:tokensに追加
- shadow-xsをOKLCH化しダークモード対応
- shadow-xsをform control用トークンとして公式化
- spacing CSS変数とTailwindクラスの対応を明記
- storybookスキルにDisabled Story推奨とDS早見表を追加

**Storybook（6件）**

- dark mode自動テスト環境を新設
- darkテーマのhex値をwarm neutral oklchトークンに合わせて更新
- layoutArchitecture・calendarResponsive Storyを追加
- レスポンシブドキュメント追加 + ファイルパス参照を修正
- props inventory一覧ページを追加

**UI修正（9件）**

- 設定ページ下部リンクのパディングを上部メニューと統一
- アップグレードボタンをprimaryバリアントに変更
- クロノタイプフェーズで時間ラベルのfont-bold除去 + tabular-nums追加
- モバイルヘッダーの日付フォントサイズをカレンダーと統一
- 統計モバイルヘッダーのsticky化と通知設定アイコンサイズ修正
- 現在時刻ラベルにtabular-numsを追加し数字幅を等幅に
- モバイルミニカレンダーの本日ハイライトサイズをPCと統一
- avatarsバケット未作成によるアイコンアップロード失敗を修正
- yearlyHeatmapのgap-[2px]をgap-0に修正

#### Claudeの所感

**「品質」が仕組みに落ちた日:**
コピーライティングルール、コントラスト監査、バンドルサイズバジェット、lint:tokensの検出ルール拡張。これまで人の目で判断していた品質基準が、ルールとして策定されlintやCIで自動検証される仕組みに変わった。1人開発だからこそ、自分が見落とす部分を仕組みでカバーする必要がある。

**コピーライティングという"見えない品質"への投資:**
UIの文言トーンを14ファイルにわたって統一している。機能には影響しないが、「穏やかで親しみやすい」というブランドトーンがUI全体に行き渡ることで、ユーザーが感じるプロダクトの印象は確実に変わる。Before/Afterストーリーを残しているのも、なぜこの言い回しを選んだかが後から追えて良い。

**PWAが"ちゃんとしたアプリ"になった:**
アイコン・スプラッシュ・マニフェスト・オフラインバナーをデザインシステムに同期させたことで、ホーム画面に追加したときの体験がネイティブアプリに近づいた。特にオフラインバナーの「穏やかな表示」への改修は、Dayoptのトーンを最後の1ピクセルまで貫く姿勢。

---

## 2026-04-01

### v0.24.0リリース・lint max-warnings 0達成・コンポーネント再配置・Storybook整備

v0.24.0をリリース。lint max-warningsを149→0に引き下げ、arbitrary valueとデザインシステム違反を全件解消。コンポーネントの配置をスコープに合わせて再整理し、Storybookにproduct overview・domain glossary・developer mapを追加。

#### やったこと

**v0.24.0リリース（2件）**

- version bumpとリリースPR (#1029)

**lint完全クリーン化（8件）**

- max-warnings 149 → 0 に引き下げ
- arbitrary valueをTailwind標準クラスに置換
- 正当なarbitrary valueにeslint-disableコメント追加
- storiesファイルのno-arbitrary-valueルールを除外
- デザインシステム違反を修正（rounded-md/xl → rounded-lg）
- `dark:brightness-[0.2]` を `dark:brightness-20` に統一
- hoverパターンをM3ステートトークンに統一
- タッチターゲット44px未満のボタンにpseudo-element追加（a11y）

**コンポーネント再配置（4件）**

- dateRangeDisplayを `ui/` → `common/` へ移動（store coupling除去）
- emptyStateを `ui/` → `common/` へ移動（statsのinline実装を置換）
- infoBoxを `common/` → `features/settings/` へ移動
- cookieConsentBannerを `ui/` → `shell/components/` へ移動

**Storybook整備（8件）**

- product overviewとdomain glossaryを追加
- developer mapとcommon pitfallsを追加
- patternsテンプレートを実態に合わせて更新
- confirmation patternをConfirmDialogベースに書き換え
- emptyStates patternを実EmptyStateコンポーネントに移行
- search patternのタグ色ドットをTagIconに移行
- errorPages patternから不要なautodocsタグを除去
- 未カバーコンポーネントのstoryを追加

**ドキュメント・用語統一（5件）**

- z-index整理（1099→1050）+ transition/elevation関係追記
- サイドバータイトルを英語に統一、HooksPatternを移動
- plans/records旧用語をentriesに統一（ADR-001準拠）
- sectionCard / labeledRowのJSDocにscope注釈を追加
- カラートークンの抜け・不整合を修正

**カレンダー・ナビゲーション修正（8件）**

- モバイルでのviewType競合を防止
- ブロック隙間をheight内側に統一（グリッド線ズレ修正）
- 現在時刻インジケータの左右パディングを4px→8pxに拡大
- 日付ハイライトをsize-6（24px）に統一
- calendar↔stats切り替えをクライアントサイドレンダリングに統一
- stats遷移をrouter.pushに変更
- entryCardのposition heightをグリッド計算に合わせて修正
- 未使用のMobileMenuButtonを削除

#### Claudeの所感

**max-warnings 0は"壊れ窓理論"の実践:**
149件のwarningを0にするのは地味だが、warningが1件でも残っていると「まあいいか」が蓄積する。0を達成したことで、今後新しいwarningが出たら即座にCIが落ちる。デザインシステムの一貫性を維持する仕組みとしてlintが機能し始めた。

**コンポーネント再配置の意図が明確:**
`ui/` → `common/`（store依存除去）、`common/` → `features/`（スコープ限定）、`ui/` → `shell/`（配置の正規化）。闇雲に動かすのではなく、各コンポーネントの依存関係とスコープに基づいて適切な場所に再配置している。これはコードベースが成長しても迷子にならないための投資。

**Storybookが"開発ドキュメント"になりつつある:**
product overview・domain glossary・developer mapの追加により、Storybookがコンポーネントカタログを超えて、プロダクト全体の理解を助けるドキュメントハブになっている。新しいメンバーが入ったときのオンボーディングにも使える資産になりつつある。
